/**
 * Google Fonts implementations
 * 
 * This file is automatically populated when Google Fonts are installed.
 * Before installation, it exports an empty object to prevent TypeScript errors.
 * 
 * To install actual Google Fonts, run:
 * deno task fonts:google:install
 */

// Empty export when fonts are not installed
export default {};
