// "use client";

// import React, {
//   CSSProperties,
//   useCallback,
//   useEffect,
//   useMemo,
//   useRef,
//   useState,
// } from "react";
// import { animate, AnimatePresence, motion, Variants } from "framer-motion";
// import { SharedProps } from "@/types";
// import { getRandomKitColors, kit } from "@/inspatial/util";
// import { PatternProps, PatternVariant, PatternVariants } from "./variant";
// import { AnimationStyle } from "@/inspatial/kit/effect";
// import { KitStaticColors } from "@/inspatial/kit/theme/color/static";
// import { Button } from "@/components/ui/button";
// import { createPortal } from "react-dom";

// /*##############################################(PATTERN UTILITIES)##############################################*/

// // Debounce function to prevent too frequent pattern changes
// const debounce = (func: Function, wait: number) => {
//   let timeout: NodeJS.Timeout;
//   return function executedFunction(...args: any[]) {
//     const later = () => {
//       clearTimeout(timeout);
//       func(...args);
//     };
//     clearTimeout(timeout);
//     timeout = setTimeout(later, wait);
//   };
// };
// // Efficient Fisher-Yates shuffle algorithm
// const shufflePatterns = (patterns: PatternVariant[]): PatternVariant[] => {
//   const shuffled = [...patterns];
//   for (let i = shuffled.length - 1; i > 0; i--) {
//     const j = Math.floor(Math.random() * (i + 1));
//     [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
//   }
//   return shuffled;
// };

// /*##############################################(ANIMATION VARIANTS)##############################################*/
// // TODO: make sure to combine all of inspatial animation variants into one
// // to reduce fragmentation e.g Text component has its own animation variants etc...

// const animationVariants: Record<AnimationStyle, Variants> = {
//   none: {
//     initial: {},
//     animate: {},
//     exit: {},
//   },
//   slideRight: {
//     initial: { x: 0 },
//     animate: { x: 0 },
//     exit: { x: "100%" },
//   },
//   fadeOut: {
//     initial: { opacity: 1 },
//     animate: { opacity: 1 },
//     exit: { opacity: 0 },
//   },
//   scaleDown: {
//     initial: { scale: 1 },
//     animate: { scale: 1 },
//     exit: { scale: 0 },
//   },
//   rotate: {},
//   flip: {},
//   pullUp: {},
//   fadeUp: {},
//   fadeDown: {},
//   fadeLeft: {},
//   fadeRight: {},
//   fadeIn: {},
//   reveal: {},
//   blurIn: {},
//   typing: {},
//   generate: {},
//   ticker: {},
//   reUp: {},
//   gradual: {},
// };

// /*##############################################(RIPPLE)##############################################*/
// interface RippleProps extends SharedProps, PatternProps {
//   mainCircleSize?: number;
//   mainCircleOpacity?: number;
//   numCircles?: number;
//   showPatterns?: boolean;
//   animate?: AnimationStyle;
// }

// function Ripple({
//   mainCircleSize = 210,
//   mainCircleOpacity = 0.24,
//   numCircles = 8,
// }: RippleProps) {
//   return Array.from({ length: numCircles }, (_, i) => {
//     const size = mainCircleSize + i * 70;
//     const opacity = mainCircleOpacity - i * 0.03;
//     const borderStyle = i === numCircles - 1 ? "dashed" : "solid";
//     const borderOpacity = 5 + i * 5;
//     return (
//       //****************** (Ripple Variant) ******************
//       <motion.div
//         key={i}
//         // @ts-ignore
//         className="absolute rounded-full bg-inherit shadow-xl border z-[-1000]"
//         style={
//           {
//             width: `${size}px`,
//             height: `${size}px`,
//             opacity: opacity,
//             borderStyle: borderStyle,
//             borderWidth: "2px",
//             // borderColor: ` ${borderOpacity / 100})`,
//           } as CSSProperties
//         }
//         initial={{ scale: 0, opacity: 0 }}
//         animate={{ scale: 1, opacity: opacity }}
//         transition={{ delay: i * 0.06, duration: 0.5 }}
//       />
//     );
//   });
// }

// /*##############################################(TILE)##############################################*/

// export function Tile() {
//   const columns = 41;
//   const rows = 11;
//   return (
//     <section className="absolute z-[-1000] inset-0 [mask-image:radial-gradient(ellipse_at_center,white,transparent)]">
//       <div className="flex bg-inherit flex-shrink-0 flex-wrap justify-center items-center gap-x-px gap-y-px scale-125">
//         {Array.from({ length: rows }).map((_, row) =>
//           Array.from({ length: columns }).map((_, col) => {
//             const index = row * columns + col;
//             return (
//               <div
//                 key={`${col}-${row}`}
//                 className={`w-10 h-10 xl:w-9 xl:h-9 lg:w-8 lg:h-8 flex flex-shrink-0 rounded-[2px] ${
//                   index % 2 === 0 ? "bg-inherit" : "bg-inherit shadow-hollow"
//                 }`}
//               />
//             );
//           })
//         )}
//       </div>
//     </section>
//   );
// }

// /*##############################################(BEAM)##############################################*/

// export function Beam({ className }: SharedProps) {
//   const paths = [
//     "M-380 -189C-380 -189 -312 216 152 343C616 470 684 875 684 875",
//     "M-373 -197C-373 -197 -305 208 159 335C623 462 691 867 691 867",
//     "M-366 -205C-366 -205 -298 200 166 327C630 454 698 859 698 859",
//     "M-359 -213C-359 -213 -291 192 173 319C637 446 705 851 705 851",
//     "M-352 -221C-352 -221 -284 184 180 311C644 438 712 843 712 843",
//     "M-345 -229C-345 -229 -277 176 187 303C651 430 719 835 719 835",
//     "M-338 -237C-338 -237 -270 168 194 295C658 422 726 827 726 827",
//     "M-331 -245C-331 -245 -263 160 201 287C665 414 733 819 733 819",
//     "M-324 -253C-324 -253 -256 152 208 279C672 406 740 811 740 811",
//     "M-317 -261C-317 -261 -249 144 215 271C679 398 747 803 747 803",
//     "M-310 -269C-310 -269 -242 136 222 263C686 390 754 795 754 795",
//     "M-303 -277C-303 -277 -235 128 229 255C693 382 761 787 761 787",
//     "M-296 -285C-296 -285 -228 120 236 247C700 374 768 779 768 779",
//     "M-289 -293C-289 -293 -221 112 243 239C707 366 775 771 775 771",
//     "M-282 -301C-282 -301 -214 104 250 231C714 358 782 763 782 763",
//     "M-275 -309C-275 -309 -207 96 257 223C721 350 789 755 789 755",
//     "M-268 -317C-268 -317 -200 88 264 215C728 342 796 747 796 747",
//     "M-261 -325C-261 -325 -193 80 271 207C735 334 803 739 803 739",
//     "M-254 -333C-254 -333 -186 72 278 199C742 326 810 731 810 731",
//     "M-247 -341C-247 -341 -179 64 285 191C749 318 817 723 817 723",
//     "M-240 -349C-240 -349 -172 56 292 183C756 310 824 715 824 715",
//     "M-233 -357C-233 -357 -165 48 299 175C763 302 831 707 831 707",
//     "M-226 -365C-226 -365 -158 40 306 167C770 294 838 699 838 699",
//     "M-219 -373C-219 -373 -151 32 313 159C777 286 845 691 845 691",
//     "M-212 -381C-212 -381 -144 24 320 151C784 278 852 683 852 683",
//     "M-205 -389C-205 -389 -137 16 327 143C791 270 859 675 859 675",
//     "M-198 -397C-198 -397 -130 8 334 135C798 262 866 667 866 667",
//     "M-191 -405C-191 -405 -123 0 341 127C805 254 873 659 873 659",
//     "M-184 -413C-184 -413 -116 -8 348 119C812 246 880 651 880 651",
//     "M-177 -421C-177 -421 -109 -16 355 111C819 238 887 643 887 643",
//     "M-170 -429C-170 -429 -102 -24 362 103C826 230 894 635 894 635",
//     "M-163 -437C-163 -437 -95 -32 369 95C833 222 901 627 901 627",
//     "M-156 -445C-156 -445 -88 -40 376 87C840 214 908 619 908 619",
//     "M-149 -453C-149 -453 -81 -48 383 79C847 206 915 611 915 611",
//     "M-142 -461C-142 -461 -74 -56 390 71C854 198 922 603 922 603",
//     "M-135 -469C-135 -469 -67 -64 397 63C861 190 929 595 929 595",
//     "M-128 -477C-128 -477 -60 -72 404 55C868 182 936 587 936 587",
//     "M-121 -485C-121 -485 -53 -80 411 47C875 174 943 579 943 579",
//     "M-114 -493C-114 -493 -46 -88 418 39C882 166 950 571 950 571",
//     "M-107 -501C-107 -501 -39 -96 425 31C889 158 957 563 957 563",
//     "M-100 -509C-100 -509 -32 -104 432 23C896 150 964 555 964 555",
//     "M-93 -517C-93 -517 -25 -112 439 15C903 142 971 547 971 547",
//     "M-86 -525C-86 -525 -18 -120 446 7C910 134 978 539 978 539",
//     "M-79 -533C-79 -533 -11 -128 453 -1C917 126 985 531 985 531",
//     "M-72 -541C-72 -541 -4 -136 460 -9C924 118 992 523 992 523",
//     "M-65 -549C-65 -549 3 -144 467 -17C931 110 999 515 999 515",
//     "M-58 -557C-58 -557 10 -152 474 -25C938 102 1006 507 1006 507",
//     "M-51 -565C-51 -565 17 -160 481 -33C945 94 1013 499 1013 499",
//     "M-44 -573C-44 -573 24 -168 488 -41C952 86 1020 491 1020 491",
//     "M-37 -581C-37 -581 31 -176 495 -49C959 78 1027 483 1027 483",
//   ];
//   return (
//     <div
//       className={kit(
//         "absolute z-[-1000] h-full w-full inset-0 [mask-size:40px] [mask-repeat:no-repeat] flex items-center justify-center",
//         className
//       )}
//     >
//       <svg
//         className=" z-0 h-full w-full pointer-events-none absolute "
//         width="100%"
//         height="100%"
//         viewBox="0 0 696 316"
//         fill="none"
//         xmlns="http://www.w3.org/2000/svg"
//       >
//         <path
//           d="M-380 -189C-380 -189 -312 216 152 343C616 470 684 875 684 875M-373 -197C-373 -197 -305 208 159 335C623 462 691 867 691 867M-366 -205C-366 -205 -298 200 166 327C630 454 698 859 698 859M-359 -213C-359 -213 -291 192 173 319C637 446 705 851 705 851M-352 -221C-352 -221 -284 184 180 311C644 438 712 843 712 843M-345 -229C-345 -229 -277 176 187 303C651 430 719 835 719 835M-338 -237C-338 -237 -270 168 194 295C658 422 726 827 726 827M-331 -245C-331 -245 -263 160 201 287C665 414 733 819 733 819M-324 -253C-324 -253 -256 152 208 279C672 406 740 811 740 811M-317 -261C-317 -261 -249 144 215 271C679 398 747 803 747 803M-310 -269C-310 -269 -242 136 222 263C686 390 754 795 754 795M-303 -277C-303 -277 -235 128 229 255C693 382 761 787 761 787M-296 -285C-296 -285 -228 120 236 247C700 374 768 779 768 779M-289 -293C-289 -293 -221 112 243 239C707 366 775 771 775 771M-282 -301C-282 -301 -214 104 250 231C714 358 782 763 782 763M-275 -309C-275 -309 -207 96 257 223C721 350 789 755 789 755M-268 -317C-268 -317 -200 88 264 215C728 342 796 747 796 747M-261 -325C-261 -325 -193 80 271 207C735 334 803 739 803 739M-254 -333C-254 -333 -186 72 278 199C742 326 810 731 810 731M-247 -341C-247 -341 -179 64 285 191C749 318 817 723 817 723M-240 -349C-240 -349 -172 56 292 183C756 310 824 715 824 715M-233 -357C-233 -357 -165 48 299 175C763 302 831 707 831 707M-226 -365C-226 -365 -158 40 306 167C770 294 838 699 838 699M-219 -373C-219 -373 -151 32 313 159C777 286 845 691 845 691M-212 -381C-212 -381 -144 24 320 151C784 278 852 683 852 683M-205 -389C-205 -389 -137 16 327 143C791 270 859 675 859 675M-198 -397C-198 -397 -130 8 334 135C798 262 866 667 866 667M-191 -405C-191 -405 -123 0 341 127C805 254 873 659 873 659M-184 -413C-184 -413 -116 -8 348 119C812 246 880 651 880 651M-177 -421C-177 -421 -109 -16 355 111C819 238 887 643 887 643M-170 -429C-170 -429 -102 -24 362 103C826 230 894 635 894 635M-163 -437C-163 -437 -95 -32 369 95C833 222 901 627 901 627M-156 -445C-156 -445 -88 -40 376 87C840 214 908 619 908 619M-149 -453C-149 -453 -81 -48 383 79C847 206 915 611 915 611M-142 -461C-142 -461 -74 -56 390 71C854 198 922 603 922 603M-135 -469C-135 -469 -67 -64 397 63C861 190 929 595 929 595M-128 -477C-128 -477 -60 -72 404 55C868 182 936 587 936 587M-121 -485C-121 -485 -53 -80 411 47C875 174 943 579 943 579M-114 -493C-114 -493 -46 -88 418 39C882 166 950 571 950 571M-107 -501C-107 -501 -39 -96 425 31C889 158 957 563 957 563M-100 -509C-100 -509 -32 -104 432 23C896 150 964 555 964 555M-93 -517C-93 -517 -25 -112 439 15C903 142 971 547 971 547M-86 -525C-86 -525 -18 -120 446 7C910 134 978 539 978 539M-79 -533C-79 -533 -11 -128 453 -1C917 126 985 531 985 531M-72 -541C-72 -541 -4 -136 460 -9C924 118 992 523 992 523M-65 -549C-65 -549 3 -144 467 -17C931 110 999 515 999 515M-58 -557C-58 -557 10 -152 474 -25C938 102 1006 507 1006 507M-51 -565C-51 -565 17 -160 481 -33C945 94 1013 499 1013 499M-44 -573C-44 -573 24 -168 488 -41C952 86 1020 491 1020 491M-37 -581C-37 -581 31 -176 495 -49C959 78 1027 483 1027 483M-30 -589C-30 -589 38 -184 502 -57C966 70 1034 475 1034 475M-23 -597C-23 -597 45 -192 509 -65C973 62 1041 467 1041 467M-16 -605C-16 -605 52 -200 516 -73C980 54 1048 459 1048 459M-9 -613C-9 -613 59 -208 523 -81C987 46 1055 451 1055 451M-2 -621C-2 -621 66 -216 530 -89C994 38 1062 443 1062 443M5 -629C5 -629 73 -224 537 -97C1001 30 1069 435 1069 435M12 -637C12 -637 80 -232 544 -105C1008 22 1076 427 1076 427M19 -645C19 -645 87 -240 551 -113C1015 14 1083 419 1083 419"
//           stroke="url(#paint0_radial_242_278)"
//           strokeOpacity="0.05"
//           strokeWidth="0.5"
//         ></path>

//         {paths.map((path, index) => (
//           <motion.path
//             key={`path-` + index}
//             d={path}
//             stroke={`url(#linearGradient-${index})`}
//             strokeOpacity="0.4"
//             strokeWidth="0.5"
//           ></motion.path>
//         ))}
//         <defs>
//           {paths.map((path, index) => (
//             <motion.linearGradient
//               id={`linearGradient-${index}`}
//               key={`gradient-${index}`}
//               initial={{
//                 x1: "0%",
//                 x2: "0%",
//                 y1: "0%",
//                 y2: "0%",
//               }}
//               animate={{
//                 x1: ["0%", "100%"],
//                 x2: ["0%", "95%"],
//                 y1: ["0%", "100%"],
//                 y2: ["0%", `${93 + Math.random() * 8}%`],
//               }}
//               transition={{
//                 duration: Math.random() * 10 + 10,
//                 ease: "easeInOut",
//                 repeat: Infinity,
//                 delay: Math.random() * 10,
//               }}
//             >
//               <stop stopColor="#18CCFC" stopOpacity="0"></stop>
//               <stop stopColor="#18CCFC"></stop>
//               <stop offset="32.5%" stopColor="#6344F5"></stop>
//               <stop offset="100%" stopColor="#AE48FF" stopOpacity="0"></stop>
//             </motion.linearGradient>
//           ))}

//           <radialGradient
//             id="paint0_radial_242_278"
//             cx="0"
//             cy="0"
//             r="1"
//             gradientUnits="userSpaceOnUse"
//             gradientTransform="translate(352 34) rotate(90) scale(555 1560.62)"
//           >
//             <stop offset="0.0666667" stopColor="var(--neutral-300)"></stop>
//             <stop offset="0.243243" stopColor="var(--neutral-300)"></stop>
//             <stop offset="0.43594" stopColor="white" stopOpacity="0"></stop>
//           </radialGradient>
//         </defs>
//       </svg>
//     </div>
//   );
// }

// /*##############################################(RAIN)##############################################*/

// export const Rain = ({ children, className }: SharedProps) => {
//   const containerRef = useRef<HTMLDivElement>(null);
//   const parentRef = useRef<HTMLDivElement>(null);

//   const beams = [
//     {
//       initialX: 10,
//       translateX: 10,
//       duration: 7,
//       repeatDelay: 3,
//       delay: 2,
//     },
//     {
//       initialX: 600,
//       translateX: 600,
//       duration: 3,
//       repeatDelay: 3,
//       delay: 4,
//     },
//     {
//       initialX: 100,
//       translateX: 100,
//       duration: 7,
//       repeatDelay: 7,
//       className: "h-6",
//     },
//     {
//       initialX: 400,
//       translateX: 400,
//       duration: 5,
//       repeatDelay: 14,
//       delay: 4,
//     },
//     {
//       initialX: 800,
//       translateX: 800,
//       duration: 11,
//       repeatDelay: 2,
//       className: "h-20",
//     },
//     {
//       initialX: 1000,
//       translateX: 1000,
//       duration: 4,
//       repeatDelay: 2,
//       className: "h-12",
//     },
//     {
//       initialX: 1200,
//       translateX: 1200,
//       duration: 6,
//       repeatDelay: 4,
//       delay: 2,
//       className: "h-6",
//     },
//   ];

//   return (
//     <div
//       ref={parentRef}
//       className={kit(
//         "flex absolute h-screen z-[-1000] bg-inherit items-center w-full justify-center overflow-hidden",
//         className
//       )}
//     >
//       {beams.map((beam) => (
//         <CollisionMechanism
//           key={beam.initialX + "beam-idx"}
//           beamOptions={beam}
//           containerRef={containerRef as React.RefObject<HTMLDivElement>}
//           parentRef={parentRef as React.RefObject<HTMLDivElement>}
//         />
//       ))}

//       {children}
//       <div
//         ref={containerRef}
//         className="absolute bg-green bottom-0 w-full inset-x-0 pointer-events-none"
//         style={{
//           boxShadow:
//             "0 0 24px rgba(34, 42, 53, 0.06), 0 1px 1px rgba(0, 0, 0, 0.05), 0 0 0 1px rgba(34, 42, 53, 0.04), 0 0 4px rgba(34, 42, 53, 0.08), 0 16px 68px rgba(47, 48, 55, 0.05), 0 1px 0 rgba(255, 255, 255, 0.1) inset",
//         }}
//       ></div>
//     </div>
//   );
// };

// const CollisionMechanism = React.forwardRef<
//   HTMLDivElement,
//   {
//     containerRef: React.RefObject<HTMLDivElement>;
//     parentRef: React.RefObject<HTMLDivElement>;
//     beamOptions?: {
//       initialX?: number;
//       translateX?: number;
//       initialY?: number;
//       translateY?: number;
//       rotate?: number;
//       className?: string;
//       duration?: number;
//       delay?: number;
//       repeatDelay?: number;
//     };
//   }
// >(({ parentRef, containerRef, beamOptions = {} }, ref) => {
//   const beamRef = useRef<HTMLDivElement>(null);
//   const [collision, setCollision] = useState<{
//     detected: boolean;
//     coordinates: { x: number; y: number } | null;
//   }>({
//     detected: false,
//     coordinates: null,
//   });
//   const [beamKey, setBeamKey] = useState(0);
//   const [cycleCollisionDetected, setCycleCollisionDetected] = useState(false);

//   useEffect(() => {
//     const checkCollision = () => {
//       if (
//         beamRef.current &&
//         containerRef.current &&
//         parentRef.current &&
//         !cycleCollisionDetected
//       ) {
//         const beamRect = beamRef.current.getBoundingClientRect();
//         const containerRect = containerRef.current.getBoundingClientRect();
//         const parentRect = parentRef.current.getBoundingClientRect();

//         if (beamRect.bottom >= containerRect.top) {
//           const relativeX =
//             beamRect.left - parentRect.left + beamRect.width / 2;
//           const relativeY = beamRect.bottom - parentRect.top;

//           setCollision({
//             detected: true,
//             coordinates: {
//               x: relativeX,
//               y: relativeY,
//             },
//           });
//           setCycleCollisionDetected(true);
//         }
//       }
//     };

//     const animationInterval = setInterval(checkCollision, 50);

//     return () => clearInterval(animationInterval);
//   }, [cycleCollisionDetected, containerRef, parentRef]);

//   useEffect(() => {
//     if (collision.detected && collision.coordinates) {
//       setTimeout(() => {
//         setCollision({ detected: false, coordinates: null });
//         setCycleCollisionDetected(false);
//       }, 2000);

//       setTimeout(() => {
//         setBeamKey((prevKey) => prevKey + 1);
//       }, 2000);
//     }
//   }, [collision]);

//   return (
//     <>
//       <motion.div
//         key={beamKey}
//         ref={beamRef}
//         animate="animate"
//         initial={{
//           translateY: beamOptions.initialY || "-200px",
//           translateX: beamOptions.initialX || "0px",
//           rotate: beamOptions.rotate || 0,
//         }}
//         variants={{
//           animate: {
//             translateY: beamOptions.translateY || "1800px",
//             translateX: beamOptions.translateX || "0px",
//             rotate: beamOptions.rotate || 0,
//           },
//         }}
//         transition={{
//           duration: beamOptions.duration || 8,
//           repeat: Infinity,
//           repeatType: "loop",
//           ease: "linear",
//           delay: beamOptions.delay || 0,
//           repeatDelay: beamOptions.repeatDelay || 0,
//         }}
//         className={kit(
//           "absolute left-0 top-20 m-auto h-14 w-px rounded-full bg-brand",
//           beamOptions.className
//         )}
//       />
//       <AnimatePresence>
//         {collision.detected && collision.coordinates && (
//           <Explosion
//             key={`${collision.coordinates.x}-${collision.coordinates.y}`}
//             className=""
//             style={{
//               left: `${collision.coordinates.x}px`,
//               top: `${collision.coordinates.y}px`,
//               transform: "translate(-50%, -50%)",
//             }}
//           />
//         )}
//       </AnimatePresence>
//     </>
//   );
// });

// CollisionMechanism.displayName = "CollisionMechanism";

// const Explosion = ({ ...props }: React.HTMLProps<HTMLDivElement>) => {
//   const spans = Array.from({ length: 20 }, (_, index) => ({
//     id: index,
//     initialX: 0,
//     initialY: 0,
//     directionX: Math.floor(Math.random() * 80 - 40),
//     directionY: Math.floor(Math.random() * -50 - 10),
//   }));

//   return (
//     <div {...props} className={kit("absolute z-50 h-2 w-2", props.className)}>
//       <motion.div
//         initial={{ opacity: 0 }}
//         animate={{ opacity: 1 }}
//         exit={{ opacity: 0 }}
//         transition={{ duration: 1.5, ease: "easeOut" }}
//         // @ts-ignore
//         className="absolute -inset-x-10 top-0 m-auto h-2 w-10 rounded-full bg-brand"
//       ></motion.div>
//       {spans.map((span) => (
//         <motion.span
//           key={span.id}
//           initial={{ x: span.initialX, y: span.initialY, opacity: 1 }}
//           animate={{
//             x: span.directionX,
//             y: span.directionY,
//             opacity: 0,
//           }}
//           transition={{ duration: Math.random() * 1.5 + 0.5, ease: "easeOut" }}
//           // @ts-ignore
//           className="absolute h-1 w-1 rounded-full bg-brand"
//         />
//       ))}
//     </div>
//   );
// };

// /*##############################################(CONFETTI TRAIL)##############################################*/

// interface ConfettiTrailProps extends SharedProps {
//   svgOptions?: {
//     duration?: number;
//   };
// }

// export const ConfettiTrail = ({
//   children,
//   className,
//   svgOptions,
// }: ConfettiTrailProps) => {
//   return (
//     <div
//       className={kit(
//         "flex absolute z-[-1000] h-screen w-full bg-inherit",
//         className
//       )}
//     >
//       <SVG svgOptions={svgOptions} />
//       {children}
//     </div>
//   );
// };

// const pathVariants = {
//   initial: { strokeDashoffset: 800, strokeDasharray: "50 800" },
//   animate: {
//     strokeDashoffset: 0,
//     strokeDasharray: "20 800",
//     opacity: [0, 1, 1, 0],
//   },
// };

// const SVG = ({
//   svgOptions,
// }: {
//   svgOptions?: {
//     duration?: number;
//   };
// }) => {
//   const paths = [
//     "M720 450C720 450 742.459 440.315 755.249 425.626C768.039 410.937 778.88 418.741 789.478 401.499C800.076 384.258 817.06 389.269 826.741 380.436C836.423 371.603 851.957 364.826 863.182 356.242C874.408 347.657 877.993 342.678 898.867 333.214C919.741 323.75 923.618 319.88 934.875 310.177C946.133 300.474 960.784 300.837 970.584 287.701C980.384 274.564 993.538 273.334 1004.85 263.087C1016.15 252.84 1026.42 250.801 1038.22 242.1C1050.02 233.399 1065.19 230.418 1074.63 215.721C1084.07 201.024 1085.49 209.128 1112.65 194.884C1139.8 180.64 1132.49 178.205 1146.43 170.636C1160.37 163.066 1168.97 158.613 1181.46 147.982C1193.95 137.35 1191.16 131.382 1217.55 125.645C1243.93 119.907 1234.19 118.899 1254.53 100.846C1274.86 82.7922 1275.12 92.8914 1290.37 76.09C1305.62 59.2886 1313.91 62.1868 1323.19 56.7536C1332.48 51.3204 1347.93 42.8082 1361.95 32.1468C1375.96 21.4855 1374.06 25.168 1397.08 10.1863C1420.09 -4.79534 1421.41 -3.16992 1431.52 -15.0078",
//     "M720 450C720 450 741.044 435.759 753.062 410.636C765.079 385.514 770.541 386.148 782.73 370.489C794.918 354.83 799.378 353.188 811.338 332.597C823.298 312.005 825.578 306.419 843.707 295.493C861.837 284.568 856.194 273.248 877.376 256.48C898.558 239.713 887.536 227.843 909.648 214.958C931.759 202.073 925.133 188.092 941.063 177.621C956.994 167.151 952.171 154.663 971.197 135.041C990.222 115.418 990.785 109.375 999.488 96.1291C1008.19 82.8827 1011.4 82.2181 1032.65 61.8861C1053.9 41.5541 1045.74 48.0281 1064.01 19.5798C1082.29 -8.86844 1077.21 -3.89415 1093.7 -19.66C1110.18 -35.4258 1105.91 -46.1146 1127.68 -60.2834C1149.46 -74.4523 1144.37 -72.1024 1154.18 -97.6802C1163.99 -123.258 1165.6 -111.332 1186.21 -135.809C1206.81 -160.285 1203.29 -160.861 1220.31 -177.633C1237.33 -194.406 1236.97 -204.408 1250.42 -214.196",
//     "M720 450C720 450 712.336 437.768 690.248 407.156C668.161 376.544 672.543 394.253 665.951 365.784C659.358 337.316 647.903 347.461 636.929 323.197C625.956 298.933 626.831 303.639 609.939 281.01C593.048 258.381 598.7 255.282 582.342 242.504C565.985 229.726 566.053 217.66 559.169 197.116C552.284 176.572 549.348 171.846 529.347 156.529C509.345 141.211 522.053 134.054 505.192 115.653C488.33 97.2527 482.671 82.5627 473.599 70.7833C464.527 59.0039 464.784 50.2169 447 32.0721C429.215 13.9272 436.29 0.858563 423.534 -12.6868C410.777 -26.2322 407.424 -44.0808 394.364 -56.4916C381.303 -68.9024 373.709 -72.6804 365.591 -96.1992C357.473 -119.718 358.364 -111.509 338.222 -136.495C318.08 -161.481 322.797 -149.499 315.32 -181.761C307.843 -214.023 294.563 -202.561 285.795 -223.25C277.026 -243.94 275.199 -244.055 258.602 -263.871",
//     "M720 450C720 450 738.983 448.651 790.209 446.852C841.436 445.052 816.31 441.421 861.866 437.296C907.422 433.172 886.273 437.037 930.656 436.651C975.04 436.264 951.399 432.343 1001.57 425.74C1051.73 419.138 1020.72 425.208 1072.85 424.127C1124.97 423.047 1114.39 420.097 1140.02 414.426C1165.65 408.754 1173.1 412.143 1214.55 411.063C1256.01 409.983 1242.78 406.182 1285.56 401.536C1328.35 396.889 1304.66 400.796 1354.41 399.573C1404.16 398.35 1381.34 394.315 1428.34 389.376C1475.35 384.438 1445.96 386.509 1497.93 385.313C1549.9 384.117 1534.63 382.499 1567.23 381.48",
//     "M720 450C720 450 696.366 458.841 682.407 472.967C668.448 487.093 673.23 487.471 647.919 492.882C622.608 498.293 636.85 499.899 609.016 512.944C581.182 525.989 596.778 528.494 571.937 533.778C547.095 539.062 551.762 548.656 536.862 556.816C521.962 564.975 515.626 563.279 497.589 575.159C479.552 587.04 484.343 590.435 461.111 598.728C437.879 607.021 442.512 605.226 423.603 618.397C404.694 631.569 402.411 629.541 390.805 641.555C379.2 653.568 369.754 658.175 353.238 663.929C336.722 669.683 330.161 674.689 312.831 684.116C295.5 693.543 288.711 698.815 278.229 704.041C267.747 709.267 258.395 712.506 240.378 726.65C222.361 740.795 230.097 738.379 203.447 745.613C176.797 752.847 193.747 752.523 166.401 767.148C139.056 781.774 151.342 783.641 130.156 791.074C108.97 798.507 116.461 802.688 96.0974 808.817C75.7334 814.946 83.8553 819.505 59.4513 830.576C35.0473 841.648 48.2548 847.874 21.8337 853.886C-4.58739 859.898 10.5966 869.102 -16.396 874.524",
//     "M720 450C720 450 695.644 482.465 682.699 506.197C669.755 529.929 671.059 521.996 643.673 556.974C616.286 591.951 625.698 590.8 606.938 615.255C588.178 639.71 592.715 642.351 569.76 665.92C546.805 689.49 557.014 687.498 538.136 722.318C519.258 757.137 520.671 760.818 503.256 774.428C485.841 788.038 491.288 790.063 463.484 831.358C435.681 872.653 437.554 867.001 425.147 885.248C412.74 903.495 411.451 911.175 389.505 934.331C367.559 957.486 375.779 966.276 352.213 990.918C328.647 1015.56 341.908 1008.07 316.804 1047.24C291.699 1086.42 301.938 1060.92 276.644 1100.23C251.349 1139.54 259.792 1138.78 243.151 1153.64",
//     "M719.974 450C719.974 450 765.293 459.346 789.305 476.402C813.318 493.459 825.526 487.104 865.093 495.586C904.659 504.068 908.361 510.231 943.918 523.51C979.475 536.789 963.13 535.277 1009.79 547.428C1056.45 559.579 1062.34 555.797 1089.82 568.96C1117.31 582.124 1133.96 582.816 1159.12 592.861C1184.28 602.906 1182.84 603.359 1233.48 614.514C1284.12 625.67 1254.63 632.207 1306.33 644.465C1358.04 656.723 1359.27 656.568 1378.67 670.21C1398.07 683.852 1406.16 676.466 1456.34 692.827C1506.51 709.188 1497.73 708.471 1527.54 715.212",
//     "M720 450C720 450 727.941 430.821 734.406 379.251C740.87 327.681 742.857 359.402 757.864 309.798C772.871 260.194 761.947 271.093 772.992 244.308C784.036 217.524 777.105 200.533 786.808 175.699C796.511 150.864 797.141 144.333 808.694 107.307C820.247 70.2821 812.404 88.4169 819.202 37.1016C826 -14.2137 829.525 -0.990829 839.341 -30.3874C849.157 -59.784 844.404 -61.5924 855.042 -98.7516C865.68 -135.911 862.018 -144.559 876.924 -167.488C891.83 -190.418 886.075 -213.535 892.87 -237.945C899.664 -262.355 903.01 -255.031 909.701 -305.588C916.393 -356.144 917.232 -330.612 925.531 -374.777",
//     "M720 450C720 450 722.468 499.363 726.104 520.449C729.739 541.535 730.644 550.025 738.836 589.07C747.028 628.115 743.766 639.319 746.146 659.812C748.526 680.306 754.006 693.598 757.006 732.469C760.007 771.34 760.322 765.244 763.893 805.195C767.465 845.146 769.92 822.227 773.398 868.469C776.875 914.71 776.207 901.365 778.233 940.19C780.259 979.015 782.53 990.477 787.977 1010.39C793.424 1030.3 791.788 1060.01 797.243 1082.24C802.698 1104.47 801.758 1130.29 808.181 1149.64C814.604 1168.99 813.135 1171.5 818.026 1225.28C822.918 1279.06 820.269 1267.92 822.905 1293.75",
//     "M720 450C720 450 737.033 492.46 757.251 515.772C777.468 539.084 768.146 548.687 785.517 570.846C802.887 593.005 814.782 609.698 824.589 634.112C834.395 658.525 838.791 656.702 855.55 695.611C872.31 734.519 875.197 724.854 890.204 764.253C905.21 803.653 899.844 790.872 919.927 820.763C940.01 850.654 939.071 862.583 954.382 886.946C969.693 911.309 968.683 909.254 993.997 945.221C1019.31 981.187 1006.67 964.436 1023.49 1007.61C1040.32 1050.79 1046.15 1038.25 1059.01 1073.05C1071.88 1107.86 1081.39 1096.19 1089.45 1131.96C1097.51 1167.73 1106.52 1162.12 1125.77 1196.89",
//     "M720 450C720 450 687.302 455.326 670.489 467.898C653.676 480.47 653.159 476.959 626.58 485.127C600.002 493.295 599.626 495.362 577.94 503.841C556.254 512.319 556.35 507.426 533.958 517.44C511.566 527.454 505.82 526.441 486.464 539.172C467.108 551.904 461.312 546.36 439.357 553.508C417.402 560.657 406.993 567.736 389.393 572.603C371.794 577.47 371.139 583.76 344.54 587.931C317.941 592.102 327.375 593.682 299.411 607.275C271.447 620.868 283.617 615.022 249.868 622.622C216.119 630.223 227.07 630.86 203.77 638.635C180.47 646.41 168.948 652.487 156.407 657.28C143.866 662.073 132.426 669.534 110.894 675.555C89.3615 681.575 90.3234 680.232 61.1669 689.897C32.0105 699.562 34.3696 702.021 15.9011 709.789C-2.56738 717.558 2.38861 719.841 -29.9494 729.462C-62.2873 739.083 -52.5552 738.225 -77.4307 744.286",
//     "M720 450C720 450 743.97 465.061 754.884 490.648C765.798 516.235 781.032 501.34 791.376 525.115C801.72 548.889 808.417 538.333 829.306 564.807C850.195 591.281 852.336 582.531 865.086 601.843C877.835 621.155 874.512 621.773 902.383 643.857C930.255 665.94 921.885 655.976 938.025 681.74C954.164 707.505 959.384 709.719 977.273 720.525C995.162 731.33 994.233 731.096 1015.92 757.676C1037.61 784.257 1025.74 768.848 1047.82 795.343C1069.91 821.837 1065.95 815.45 1085.93 834.73C1105.91 854.009 1110.53 848.089 1124.97 869.759C1139.4 891.428 1140.57 881.585 1158.53 911.499C1176.5 941.414 1184.96 933.829 1194.53 948.792C1204.09 963.755 1221.35 973.711 1232.08 986.224C1242.8 998.738 1257.34 1015.61 1269.99 1026.53C1282.63 1037.45 1293.81 1040.91 1307.21 1064.56",
//     "M720 450C720 450 718.24 412.717 716.359 397.31C714.478 381.902 713.988 362.237 710.785 344.829C707.582 327.42 708.407 322.274 701.686 292.106C694.965 261.937 699.926 270.857 694.84 240.765C689.753 210.674 693.055 217.076 689.674 184.902C686.293 152.728 686.041 149.091 682.676 133.657C679.311 118.223 682.23 106.005 681.826 80.8297C681.423 55.6545 677.891 60.196 675.66 30.0226C673.429 -0.150848 672.665 -7.94842 668.592 -26.771C664.52 -45.5935 664.724 -43.0755 661.034 -78.7766C657.343 -114.478 658.509 -103.181 653.867 -133.45C649.226 -163.719 650.748 -150.38 647.052 -182.682C643.357 -214.984 646.125 -214.921 645.216 -238.402C644.307 -261.883 640.872 -253.4 637.237 -291.706C633.602 -330.012 634.146 -309.868 630.717 -343.769C627.288 -377.669 628.008 -370.682 626.514 -394.844",
//     "M720 450C720 450 730.384 481.55 739.215 507.557C748.047 533.564 751.618 537.619 766.222 562.033C780.825 586.447 774.187 582.307 787.606 618.195C801.025 654.082 793.116 653.536 809.138 678.315C825.16 703.095 815.485 717.073 829.898 735.518C844.311 753.964 845.351 773.196 852.197 786.599C859.042 800.001 862.876 805.65 872.809 845.974C882.742 886.297 885.179 874.677 894.963 903.246C904.747 931.816 911.787 924.243 921.827 961.809C931.867 999.374 927.557 998.784 940.377 1013.59C953.197 1028.4 948.555 1055.77 966.147 1070.54C983.739 1085.31 975.539 1105.69 988.65 1125.69C1001.76 1145.69 1001.82 1141.59 1007.54 1184.37C1013.27 1227.15 1018.98 1198.8 1029.67 1241.58",
//     "M720 450C720 450 684.591 447.135 657.288 439.014C629.985 430.894 618.318 435.733 600.698 431.723C583.077 427.714 566.975 425.639 537.839 423.315C508.704 420.991 501.987 418.958 476.29 413.658C450.592 408.359 460.205 410.268 416.97 408.927C373.736 407.586 396.443 401.379 359.262 396.612C322.081 391.844 327.081 393.286 300.224 391.917C273.368 390.547 264.902 385.49 241.279 382.114C217.655 378.739 205.497 378.95 181.98 377.253C158.464 375.556 150.084 369.938 117.474 366.078C84.8644 362.218 81.5401 361.501 58.8734 358.545C36.2067 355.59 33.6442 351.938 -3.92281 346.728C-41.4898 341.519 -18.6466 345.082 -61.4654 341.179C-104.284 337.275 -102.32 338.048 -121.821 332.369",
//     "M720 450C720 450 714.384 428.193 708.622 410.693C702.86 393.193 705.531 397.066 703.397 372.66C701.264 348.254 697.8 345.181 691.079 330.466C684.357 315.751 686.929 312.356 683.352 292.664C679.776 272.973 679.079 273.949 674.646 255.07C670.213 236.192 670.622 244.371 665.271 214.561C659.921 184.751 659.864 200.13 653.352 172.377C646.841 144.623 647.767 151.954 644.123 136.021C640.48 120.088 638.183 107.491 636.127 96.8178C634.072 86.1443 632.548 77.5871 626.743 54.0492C620.938 30.5112 622.818 28.9757 618.613 16.577C614.407 4.17831 615.555 -13.1527 608.752 -24.5691C601.95 -35.9855 603.375 -51.0511 599.526 -60.1492C595.678 -69.2472 593.676 -79.3623 587.865 -100.431C582.053 -121.5 584.628 -117.913 578.882 -139.408C573.137 -160.903 576.516 -161.693 571.966 -182.241C567.416 -202.789 567.42 -198.681 562.834 -218.28C558.248 -237.879 555.335 -240.47 552.072 -260.968C548.808 -281.466 547.605 -280.956 541.772 -296.427C535.94 -311.898 537.352 -315.211 535.128 -336.018C532.905 -356.826 531.15 -360.702 524.129 -377.124",
//     "M720 450C720 450 711.433 430.82 707.745 409.428C704.056 388.035 704.937 381.711 697.503 370.916C690.069 360.121 691.274 359.999 685.371 334.109C679.469 308.22 677.496 323.883 671.24 294.303C664.984 264.724 667.608 284.849 662.065 258.116C656.522 231.383 656.357 229.024 647.442 216.172C638.527 203.319 640.134 192.925 635.555 178.727C630.976 164.529 630.575 150.179 624.994 139.987C619.413 129.794 615.849 112.779 612.251 103.074C608.654 93.3696 606.942 85.6729 603.041 63.0758C599.14 40.4787 595.242 36.9267 589.533 23.8967C583.823 10.8666 581.18 -2.12401 576.96 -14.8333C572.739 -27.5425 572.696 -37.7703 568.334 -51.3441C563.972 -64.9179 562.14 -67.2124 556.992 -93.299C551.844 -119.386 550.685 -109.743 544.056 -129.801C537.428 -149.859 534.97 -151.977 531.034 -170.076C527.099 -188.175 522.979 -185.119 519.996 -207.061C517.012 -229.004 511.045 -224.126 507.478 -247.077C503.912 -270.029 501.417 -271.033 495.534 -287C489.651 -302.968 491.488 -300.977 484.68 -326.317C477.872 -351.657 476.704 -348.494 472.792 -363.258",
//     "M720 450C720 450 723.524 466.673 728.513 497.319C733.503 527.964 731.894 519.823 740.001 542.706C748.108 565.589 744.225 560.598 748.996 588.365C753.766 616.131 756.585 602.096 761.881 636.194C767.178 670.293 768.155 649.089 771.853 679.845C775.551 710.6 775.965 703.738 781.753 724.555C787.54 745.372 787.248 758.418 791.422 773.79C795.596 789.162 798.173 807.631 804.056 819.914C809.938 832.197 806.864 843.07 811.518 865.275C816.171 887.48 816.551 892.1 822.737 912.643C828.922 933.185 830.255 942.089 833.153 956.603C836.052 971.117 839.475 969.242 846.83 1003.98C854.185 1038.71 850.193 1028.86 854.119 1048.67C858.045 1068.48 857.963 1074.39 863.202 1094.94C868.44 1115.49 867.891 1108.03 874.497 1138.67C881.102 1169.31 880.502 1170.72 887.307 1186.56C894.111 1202.4 890.388 1209.75 896.507 1231.25C902.627 1252.76 902.54 1245.39 906.742 1279.23",
//     "M720 450C720 450 698.654 436.893 669.785 424.902C640.916 412.91 634.741 410.601 615.568 402.586C596.396 394.571 594.829 395.346 568.66 378.206C542.492 361.067 547.454 359.714 514.087 348.978C480.721 338.242 479.79 334.731 467.646 329.846C455.502 324.96 448.63 312.156 416.039 303.755C383.448 295.354 391.682 293.73 365.021 280.975C338.36 268.219 328.715 267.114 309.809 252.575C290.903 238.036 277.185 246.984 259.529 230.958C241.873 214.931 240.502 224.403 211.912 206.241C183.323 188.078 193.288 190.89 157.03 181.714C120.772 172.538 127.621 170.109 108.253 154.714C88.8857 139.319 75.4927 138.974 56.9647 132.314C38.4366 125.654 33.8997 118.704 4.77584 106.7C-24.348 94.6959 -19.1326 90.266 -46.165 81.9082",
//     "M720 450C720 450 711.596 475.85 701.025 516.114C690.455 556.378 697.124 559.466 689.441 579.079C681.758 598.693 679.099 597.524 675.382 642.732C671.665 687.94 663.4 677.024 657.844 700.179C652.288 723.333 651.086 724.914 636.904 764.536C622.723 804.158 631.218 802.853 625.414 827.056C619.611 851.259 613.734 856.28 605.94 892.262C598.146 928.244 595.403 924.314 588.884 957.785C582.364 991.255 583.079 991.176 575.561 1022.63C568.044 1054.08 566.807 1058.45 558.142 1084.32C549.476 1110.2 553.961 1129.13 542.367 1149.25C530.772 1169.37 538.268 1180.37 530.338 1207.27C522.407 1234.17 520.826 1245.53 512.156 1274.2",
//     "M720 450C720 450 730.571 424.312 761.424 411.44C792.277 398.569 772.385 393.283 804.069 377.232C835.752 361.182 829.975 361.373 848.987 342.782C867.999 324.192 877.583 330.096 890.892 303.897C904.201 277.698 910.277 282.253 937.396 264.293C964.514 246.333 949.357 246.834 978.7 230.438C1008.04 214.042 990.424 217.952 1021.51 193.853C1052.6 169.753 1054.28 184.725 1065.97 158.075C1077.65 131.425 1087.76 139.068 1111.12 120.345C1134.49 101.622 1124.9 104.858 1151.67 86.3162C1178.43 67.7741 1167.09 66.2676 1197.53 47.2606C1227.96 28.2536 1225.78 23.2186 1239.27 12.9649C1252.76 2.7112 1269.32 -9.47929 1282.88 -28.5587C1296.44 -47.6381 1305.81 -41.3853 1323.82 -62.7027C1341.83 -84.0202 1340.32 -82.3794 1368.98 -98.9326",
//   ];

//   // InSpatial Kit Colors
//   const colors = KitStaticColors;

//   return (
//     <motion.svg
//       viewBox="0 0 1440 900"
//       fill="none"
//       xmlns="http://www.w3.org/2000/svg"
//       initial={{ opacity: 0 }}
//       animate={{ opacity: 1 }}
//       transition={{ duration: 1 }}
//       className="absolute inset-0 w-full h-full"
//     >
//       {paths.map((path, idx) => (
//         <motion.path
//           d={path}
//           stroke={colors[idx]}
//           strokeWidth="2.3"
//           strokeLinecap="round"
//           variants={pathVariants}
//           initial="initial"
//           animate="animate"
//           transition={{
//             duration: svgOptions?.duration || 10,
//             ease: "linear",
//             repeat: Infinity,
//             repeatType: "loop",
//             delay: Math.floor(Math.random() * 10),
//             repeatDelay: Math.floor(Math.random() * 10 + 2),
//           }}
//           key={`path-first-${idx}`}
//         />
//       ))}

//       {/* duplicate for more paths */}
//       {paths.map((path, idx) => (
//         <motion.path
//           d={path}
//           stroke={colors[idx]}
//           strokeWidth="2.3"
//           strokeLinecap="round"
//           variants={pathVariants}
//           initial="initial"
//           animate="animate"
//           transition={{
//             duration: svgOptions?.duration || 10,
//             ease: "linear",
//             repeat: Infinity,
//             repeatType: "loop",
//             delay: Math.floor(Math.random() * 10),
//             repeatDelay: Math.floor(Math.random() * 10 + 2),
//           }}
//           key={`path-second-${idx}`}
//         />
//       ))}
//     </motion.svg>
//   );
// };

// /*##############################################(METEOR)##############################################*/

// interface MeteorsProps extends SharedProps {
//   number?: number;
// }

// export const Meteor = ({ number, className }: MeteorsProps) => {
//   const meteors = new Array(number || 20).fill(true);
//   return (
//     <>
//       {meteors.map((el, idx) => (
//         <span
//           key={"meteor" + idx}
//           className={kit(
//             "flex absolute z-[-1000] animate-meteor top-1/2 left-1/2 h-0.5 w-0.5 rounded-[9999px] bg-muted shadow-prime rotate-[215deg]",
//             "before:content-[''] before:absolute before:top-1/2 before:transform before:-translate-y-[50%] before:w-[50px] before:h-[1px] before:bg-gradient-to-r before:from-primary before:to-transparent",
//             className
//           )}
//           style={{
//             top: 0,
//             left: Math.floor(Math.random() * (400 - -400) + -400) + "px",
//             animationDelay: Math.random() * (0.8 - 0.2) + 0.2 + "s",
//             animationDuration: Math.floor(Math.random() * (10 - 2) + 2) + "s",
//           }}
//         ></span>
//       ))}
//     </>
//   );
// };

// /*##############################################(SPARKLE)##############################################*/

// interface MousePosition {
//   x: number;
//   y: number;
// }

// function MousePosition(): MousePosition {
//   const [mousePosition, setMousePosition] = useState<MousePosition>({
//     x: 0,
//     y: 0,
//   });

//   useEffect(() => {
//     const handleMouseMove = (event: MouseEvent) => {
//       setMousePosition({ x: event.clientX, y: event.clientY });
//     };

//     window.addEventListener("mousemove", handleMouseMove);

//     return () => {
//       window.removeEventListener("mousemove", handleMouseMove);
//     };
//   }, []);

//   return mousePosition;
// }

// interface SparkleProps {
//   className?: string;
//   quantity?: number;
//   staticity?: number;
//   ease?: number;
//   size?: number;
//   refresh?: boolean;
//   color?: string; // Now accepts HSL string like "hsl(var(--primary))"
//   vx?: number;
//   vy?: number;
// }

// function parseHSL(hsl: string): number[] {
//   // Handle HSL variable format
//   if (hsl.includes("var(--")) {
//     // Get the CSS variable value
//     const varName = hsl.match(/var\((.*?)\)/)?.[1] || "";
//     if (varName) {
//       const value = getComputedStyle(document.documentElement)
//         .getPropertyValue(varName)
//         .trim();
//       // Parse the HSL values, assuming format is "H S% L%"
//       const [h, s, l] = value
//         .split(" ")
//         .map((val) => parseFloat(val.replace("%", "")));
//       return [h || 0, s || 0, l || 0];
//     }
//   }

//   // Fallback to parsing regular HSL format
//   const matches = hsl.match(/\d+/g);
//   if (matches) {
//     return matches.map(Number).slice(0, 3);
//   }

//   return [0, 0, 100]; // Default to white
// }

// export const Sparkle: React.FC<SparkleProps> = ({
//   className = "",
//   quantity = 350,
//   staticity = 25,
//   ease = 50,
//   size = 0.5,
//   refresh = false,
//   color = "hsl(var(--primary))",
//   vx = 0,
//   vy = 0,
// }) => {
//   const canvasRef = useRef<HTMLCanvasElement>(null);
//   const canvasContainerRef = useRef<HTMLDivElement>(null);
//   const contextRef = useRef<CanvasRenderingContext2D | null>(null);
//   const circlesRef = useRef<Circle[]>([]);
//   const mousePositionRef = useRef<{ x: number; y: number }>({ x: 0, y: 0 });
//   const canvasSizeRef = useRef<{ w: number; h: number }>({ w: 0, h: 0 });
//   const animationFrameRef = useRef<number | null>(null);
//   const isMountedRef = useRef(true);

//   // Get device pixel ratio safely
//   const getDevicePixelRatio = useCallback(() => {
//     if (typeof window === "undefined") return 1;
//     return window.devicePixelRatio || 1;
//   }, []);

//   const dpr = getDevicePixelRatio();

//   // Handle mouse tracking without state updates for better performance
//   useEffect(() => {
//     if (typeof window === "undefined") return;

//     const handleMouseMove = (event: MouseEvent) => {
//       mousePositionRef.current = { x: event.clientX, y: event.clientY };
//     };

//     window.addEventListener("mousemove", handleMouseMove);

//     return () => {
//       window.removeEventListener("mousemove", handleMouseMove);
//     };
//   }, []);

//   // Parse HSL color values safely
//   const parseHSL = useCallback((hsl: string): [number, number, number] => {
//     try {
//       // Handle HSL variable format
//       if (hsl.includes("var(--")) {
//         // Create a test element to get computed color
//         const testEl = document.createElement("div");
//         testEl.style.color = hsl;
//         testEl.style.display = "none";
//         document.body.appendChild(testEl);

//         const computedColor = getComputedStyle(testEl).color;
//         document.body.removeChild(testEl);

//         // Parse RGB from computed color
//         const rgb = computedColor.match(/\d+/g)?.map(Number) || [0, 0, 100];
//         if (rgb.length >= 3) {
//           return [rgb[0], rgb[1], rgb[2]];
//         }
//       }

//       // Try to parse direct HSL format
//       const matches = hsl.match(/\d+/g);
//       if (matches && matches.length >= 3) {
//         return matches.map(Number).slice(0, 3) as [number, number, number];
//       }
//     } catch (err) {
//       console.warn("Error parsing color, using default:", err);
//     }

//     // Default to white
//     return [0, 0, 100];
//   }, []);

//   // Safe canvas resizing
//   const resizeCanvas = useCallback(() => {
//     if (
//       !canvasContainerRef.current ||
//       !canvasRef.current ||
//       !contextRef.current
//     )
//       return;

//     try {
//       circlesRef.current = [];

//       const container = canvasContainerRef.current;
//       const canvas = canvasRef.current;
//       const context = contextRef.current;

//       canvasSizeRef.current.w = container.offsetWidth || 300;
//       canvasSizeRef.current.h = container.offsetHeight || 150;

//       const dpr = getDevicePixelRatio();

//       canvas.width = canvasSizeRef.current.w * dpr;
//       canvas.height = canvasSizeRef.current.h * dpr;
//       canvas.style.width = `${canvasSizeRef.current.w}px`;
//       canvas.style.height = `${canvasSizeRef.current.h}px`;

//       context.scale(dpr, dpr);
//     } catch (err) {
//       console.warn("Error resizing canvas:", err);
//     }
//   }, [getDevicePixelRatio]);

//   // Create circle parameters
//   const createCircleParams = useCallback((): Circle => {
//     const { w, h } = canvasSizeRef.current;

//     const x = Math.floor(Math.random() * w);
//     const y = Math.floor(Math.random() * h);
//     const pSize = Math.floor(Math.random() * 2) + size;
//     const alpha = 0;
//     const targetAlpha = parseFloat((Math.random() * 0.6 + 0.1).toFixed(1));
//     const dx = (Math.random() - 0.5) * 0.1;
//     const dy = (Math.random() - 0.5) * 0.1;
//     const magnetism = 0.1 + Math.random() * 4;

//     return {
//       x,
//       y,
//       translateX: 0,
//       translateY: 0,
//       size: pSize,
//       alpha,
//       targetAlpha,
//       dx,
//       dy,
//       magnetism,
//     };
//   }, [size]);

//   // Draw a circle
//   const drawCircle = useCallback(
//     (circle: Circle, update = false) => {
//       const context = contextRef.current;
//       if (!context) return;

//       try {
//         const {
//           x,
//           y,
//           translateX,
//           translateY,
//           size: circleSize,
//           alpha,
//         } = circle;

//         const colorValues = parseHSL(color);
//         const h = colorValues[0];
//         const s = colorValues[1];
//         const l = colorValues[2];

//         context.save();
//         context.translate(translateX, translateY);
//         context.beginPath();
//         context.arc(x, y, circleSize, 0, 2 * Math.PI);
//         context.fillStyle = `hsla(${h}, ${s}%, ${l}%, ${alpha})`;
//         context.fill();
//         context.restore();

//         if (!update) {
//           circlesRef.current.push(circle);
//         }
//       } catch (err) {
//         console.warn("Error drawing circle:", err);
//       }
//     },
//     [color, parseHSL]
//   );

//   // Clear the canvas
//   const clearContext = useCallback(() => {
//     const context = contextRef.current;
//     if (!context) return;

//     try {
//       const { w, h } = canvasSizeRef.current;
//       context.clearRect(0, 0, w, h);
//     } catch (err) {
//       console.warn("Error clearing context:", err);
//     }
//   }, []);

//   // Draw sparkles
//   const drawSparkles = useCallback(() => {
//     if (!isMountedRef.current) return;

//     clearContext();
//     try {
//       const safeQuantity = Math.min(Math.max(quantity, 50), 1000); // Safety limits

//       for (let i = 0; i < safeQuantity; i++) {
//         const circle = createCircleParams();
//         drawCircle(circle);
//       }
//     } catch (err) {
//       console.warn("Error drawing sparkles:", err);
//     }
//   }, [clearContext, createCircleParams, drawCircle, quantity]);

//   // Remap values for edge opacity
//   const remapValue = useCallback(
//     (
//       value: number,
//       start1: number,
//       end1: number,
//       start2: number,
//       end2: number
//     ): number => {
//       try {
//         const remapped =
//           ((value - start1) * (end2 - start2)) / (end1 - start1) + start2;
//         return remapped > 0 ? remapped : 0;
//       } catch (err) {
//         return 0;
//       }
//     },
//     []
//   );

//   // Update mouse position
//   const updateMousePosition = useCallback(() => {
//     if (!canvasRef.current) return;

//     try {
//       const rect = canvasRef.current.getBoundingClientRect();
//       const { w, h } = canvasSizeRef.current;
//       const mousePos = mousePositionRef.current;

//       const x = mousePos.x - rect.left - w / 2;
//       const y = mousePos.y - rect.top - h / 2;

//       // Check if mouse is inside canvas bounds
//       if (x < w / 2 && x > -w / 2 && y < h / 2 && y > -h / 2) {
//         mousePositionRef.current = { x, y };
//       }
//     } catch (err) {
//       console.warn("Error updating mouse position:", err);
//     }
//   }, []);

//   // Animation loop with safety checks
//   const animate = useCallback(() => {
//     if (!isMountedRef.current) return;

//     try {
//       clearContext();

//       // Process existing circles with safety
//       const newCircles: Circle[] = [];

//       for (let i = 0; i < circlesRef.current.length; i++) {
//         const circle = circlesRef.current[i];

//         // Handle the alpha value with edge detection
//         const edge = [
//           circle.x + circle.translateX - circle.size,
//           canvasSizeRef.current.w - circle.x - circle.translateX - circle.size,
//           circle.y + circle.translateY - circle.size,
//           canvasSizeRef.current.h - circle.y - circle.translateY - circle.size,
//         ];

//         const closestEdge = Math.min(...edge);
//         const remapClosestEdge = parseFloat(
//           remapValue(closestEdge, 0, 20, 0, 1).toFixed(2)
//         );

//         if (remapClosestEdge > 1) {
//           circle.alpha += 0.02;
//           if (circle.alpha > circle.targetAlpha) {
//             circle.alpha = circle.targetAlpha;
//           }
//         } else {
//           circle.alpha = circle.targetAlpha * remapClosestEdge;
//         }

//         // Update position
//         circle.x += circle.dx + vx;
//         circle.y += circle.dy + vy;

//         // Apply magnetic effect
//         circle.translateX +=
//           (mousePositionRef.current.x / (staticity / circle.magnetism) -
//             circle.translateX) /
//           ease;
//         circle.translateY +=
//           (mousePositionRef.current.y / (staticity / circle.magnetism) -
//             circle.translateY) /
//           ease;

//         // Draw the updated circle
//         drawCircle(circle, true);

//         // Keep circles within bounds
//         if (
//           circle.x >= -circle.size &&
//           circle.x <= canvasSizeRef.current.w + circle.size &&
//           circle.y >= -circle.size &&
//           circle.y <= canvasSizeRef.current.h + circle.size
//         ) {
//           newCircles.push(circle);
//         }
//       }

//       // Replace old circles that went out of bounds
//       const diff = quantity - newCircles.length;
//       circlesRef.current = newCircles;

//       if (diff > 0) {
//         for (let i = 0; i < diff; i++) {
//           const circle = createCircleParams();
//           drawCircle(circle);
//         }
//       }

//       // Continue animation
//       if (isMountedRef.current) {
//         animationFrameRef.current = requestAnimationFrame(animate);
//       }
//     } catch (err) {
//       console.warn("Animation error:", err);
//       // Try to recover
//       if (isMountedRef.current) {
//         animationFrameRef.current = requestAnimationFrame(animate);
//       }
//     }
//   }, [
//     clearContext,
//     createCircleParams,
//     drawCircle,
//     ease,
//     quantity,
//     remapValue,
//     staticity,
//     vx,
//     vy,
//   ]);

//   // Initialize canvas and context
//   useEffect(() => {
//     isMountedRef.current = true;

//     if (!canvasRef.current) return;

//     try {
//       const context = canvasRef.current.getContext("2d", {
//         alpha: true,
//         desynchronized: true, // Performance optimization
//       });

//       if (context) {
//         contextRef.current = context;
//         resizeCanvas();
//         drawSparkles();
//         updateMousePosition();
//         animationFrameRef.current = requestAnimationFrame(animate);
//       }
//     } catch (err) {
//       console.warn("Error initializing canvas:", err);
//     }

//     return () => {
//       isMountedRef.current = false;
//       if (animationFrameRef.current) {
//         cancelAnimationFrame(animationFrameRef.current);
//       }
//     };
//   }, [animate, drawSparkles, resizeCanvas, updateMousePosition]);

//   // Handle resize events
//   useEffect(() => {
//     if (typeof window === "undefined") return;

//     const handleResize = () => {
//       if (isMountedRef.current) {
//         resizeCanvas();
//         drawSparkles();
//       }
//     };

//     window.addEventListener("resize", handleResize);

//     return () => {
//       window.removeEventListener("resize", handleResize);
//     };
//   }, [drawSparkles, resizeCanvas]);

//   // Handle refresh prop changes
//   useEffect(() => {
//     if (refresh && isMountedRef.current) {
//       resizeCanvas();
//       drawSparkles();
//     }
//   }, [refresh, drawSparkles, resizeCanvas]);

//   // Update mouse position on mouse movement
//   useEffect(() => {
//     updateMousePosition();
//   }, [updateMousePosition]);

//   return (
//     <div
//       className={kit(
//         "flex absolute h-full w-full z-[-1000] pointer-events-none",
//         className
//       )}
//       ref={canvasContainerRef}
//       aria-hidden="true"
//     >
//       <canvas
//         ref={canvasRef}
//         className="w-full h-full"
//         style={{ display: "block" }}
//       />
//     </div>
//   );
// };

// // Add proper interface for Circle type
// interface Circle {
//   x: number;
//   y: number;
//   translateX: number;
//   translateY: number;
//   size: number;
//   alpha: number;
//   targetAlpha: number;
//   dx: number;
//   dy: number;
//   magnetism: number;
// }

// /*##############################################(VORTEX)##############################################*/

// interface VortexProps extends SharedProps {
//   particleCount?: number;
//   rangeY?: number;
//   baseHue?: number;
//   baseSpeed?: number;
//   rangeSpeed?: number;
//   baseRadius?: number;
//   rangeRadius?: number;
// }

// export const Vortex = (props: VortexProps) => {
//   const {
//     className,
//     particleCount = 700,
//     rangeY = 100,
//     baseHue = 220,
//     baseSpeed = 0.1,
//     rangeSpeed = 0.5,
//     baseRadius = 1,
//     rangeRadius = 2,
//   } = props;

//   const canvasRef = useRef<HTMLCanvasElement>(null);
//   const containerRef = useRef<HTMLDivElement>(null);
//   const animationFrameRef = useRef<number | undefined>(undefined);

//   const particlePropCount = 9;
//   const particlePropsLength = (props.particleCount ?? 700) * particlePropCount;
//   const baseTTL = 50;
//   const rangeTTL = 150;
//   const rangeHue = 100;

//   let particleProps: Float32Array;
//   let center: [number, number] = [0, 0];
//   let tick = 0;

//   const rand = (n: number): number => n * Math.random();
//   const randRange = (n: number): number => n - rand(2 * n);
//   const fadeInOut = (t: number, m: number): number => {
//     let hm = 0.5 * m;
//     return Math.abs(((t + hm) % m) - hm) / hm;
//   };

//   const initParticle = (i: number) => {
//     const canvas = canvasRef.current;
//     if (!canvas) return;

//     let x, y, vx, vy, life, ttl, speed, radius, hue;

//     x = rand(canvas.width);
//     y = center[1] + randRange(rangeY);
//     vx = 0;
//     vy = 0;
//     life = 0;
//     ttl = baseTTL + rand(rangeTTL);
//     speed = baseSpeed + rand(rangeSpeed);
//     radius = baseRadius + rand(rangeRadius);
//     hue = baseHue + rand(rangeHue);

//     particleProps.set([x, y, vx, vy, life, ttl, speed, radius, hue], i);
//   };

//   const drawParticle = (
//     x: number,
//     y: number,
//     x2: number,
//     y2: number,
//     life: number,
//     ttl: number,
//     radius: number,
//     hue: number,
//     ctx: CanvasRenderingContext2D
//   ) => {
//     ctx.save();
//     ctx.lineCap = "round";
//     ctx.lineWidth = radius;
//     ctx.strokeStyle = `hsl(${hue} 100% 60% / ${fadeInOut(life, ttl)})`;
//     ctx.beginPath();
//     ctx.moveTo(x, y);
//     ctx.lineTo(x2, y2);
//     ctx.stroke();
//     ctx.closePath();
//     ctx.restore();
//   };

//   const updateParticle = (i: number, ctx: CanvasRenderingContext2D) => {
//     const canvas = canvasRef.current;
//     if (!canvas) return;

//     let i2 = 1 + i,
//       i3 = 2 + i,
//       i4 = 3 + i,
//       i5 = 4 + i,
//       i6 = 5 + i,
//       i7 = 6 + i,
//       i8 = 7 + i,
//       i9 = 8 + i;

//     let x = particleProps[i];
//     let y = particleProps[i2];
//     let life = particleProps[i5];
//     let ttl = particleProps[i6];
//     let speed = particleProps[i7];
//     let radius = particleProps[i8];
//     let hue = particleProps[i9];

//     let dx = x - center[0];
//     let dy = y - center[1];
//     let angle = Math.atan2(dy, dx);

//     angle += speed * 0.02;

//     let distance = Math.sqrt(dx * dx + dy * dy);
//     distance = Math.max(0, distance - speed * 0.1);

//     let x2 = center[0] + Math.cos(angle) * distance;
//     let y2 = center[1] + Math.sin(angle) * distance;

//     drawParticle(x, y, x2, y2, life, ttl, radius, hue, ctx);

//     life++;

//     particleProps[i] = x2;
//     particleProps[i2] = y2;
//     particleProps[i5] = life;

//     if (life > ttl || checkBounds(x2, y2, canvas)) {
//       initParticle(i);
//     }
//   };

//   const checkBounds = (x: number, y: number, canvas: HTMLCanvasElement) => {
//     return x > canvas.width || x < 0 || y > canvas.height || y < 0;
//   };

//   const drawParticles = (ctx: CanvasRenderingContext2D) => {
//     for (let i = 0; i < particlePropsLength; i += particlePropCount) {
//       updateParticle(i, ctx);
//     }
//   };

//   const resize = (canvas: HTMLCanvasElement, ctx: CanvasRenderingContext2D) => {
//     const { innerWidth, innerHeight } = window;

//     canvas.width = innerWidth;
//     canvas.height = innerHeight;

//     center[0] = 0.5 * canvas.width;
//     center[1] = 0.5 * canvas.height;
//   };

//   const renderGlow = (
//     canvas: HTMLCanvasElement,
//     ctx: CanvasRenderingContext2D
//   ) => {
//     ctx.save();
//     ctx.filter = "blur(8px) brightness(200%)";
//     ctx.globalCompositeOperation = "lighter";
//     ctx.drawImage(canvas, 0, 0);
//     ctx.restore();

//     ctx.save();
//     ctx.filter = "blur(4px) brightness(200%)";
//     ctx.globalCompositeOperation = "lighter";
//     ctx.drawImage(canvas, 0, 0);
//     ctx.restore();
//   };

//   const draw = (canvas: HTMLCanvasElement, ctx: CanvasRenderingContext2D) => {
//     tick++;

//     ctx.clearRect(0, 0, canvas.width, canvas.height);
//     drawParticles(ctx);
//     renderGlow(canvas, ctx);

//     animationFrameRef.current = window.requestAnimationFrame(() =>
//       draw(canvas, ctx)
//     );
//   };

//   const initParticles = () => {
//     tick = 0;
//     particleProps = new Float32Array(particlePropsLength);

//     for (let i = 0; i < particlePropsLength; i += particlePropCount) {
//       initParticle(i);
//     }
//   };

//   const setup = () => {
//     const canvas = canvasRef.current;
//     const container = containerRef.current;

//     if (canvas && container) {
//       const ctx = canvas.getContext("2d");
//       if (ctx) {
//         resize(canvas, ctx);
//         initParticles();
//         draw(canvas, ctx);
//       }
//     }
//   };

//   useEffect(() => {
//     setup();

//     const handleResize = () => {
//       const canvas = canvasRef.current;
//       const ctx = canvas?.getContext("2d");
//       if (canvas && ctx) {
//         resize(canvas, ctx);
//       }
//     };

//     window.addEventListener("resize", handleResize);

//     return () => {
//       window.removeEventListener("resize", handleResize);
//       if (animationFrameRef.current) {
//         cancelAnimationFrame(animationFrameRef.current);
//       }
//     };
//   }, [resize, setup]);

//   return (
//     <motion.div
//       initial={{ opacity: 0 }}
//       animate={{ opacity: 1 }}
//       ref={containerRef}
//       className={kit(
//         "flex absolute z-[-1000] h-screen w-screen inset-0 items-center justify-center",
//         className
//       )}
//     >
//       <canvas ref={canvasRef} />
//     </motion.div>
//   );
// };

// /*##############################################(WAVE)##############################################*/

// interface RibbonProps extends SharedProps {
//   colors?: string[];
//   ribbonWidth?: number;
//   blur?: number;
//   speed?: "slow" | "fast";
//   ribbonOpacity?: number;
// }

// export const Ribbon = ({
//   children,
//   className,
//   colors,
//   ribbonWidth,
//   blur = 10,
//   speed = "fast",
//   ribbonOpacity = 0.5,
//   ...props
// }: RibbonProps) => {
//   let w: number,
//     h: number,
//     nt: number,
//     i: number,
//     x: number,
//     ctx: CanvasRenderingContext2D | null,
//     canvas: HTMLCanvasElement | null;

//   const canvasRef = useRef<HTMLCanvasElement>(null);
//   // Store random colors in a ref to maintain consistency between renders
//   const randomColorsRef = useRef<string[]>(getRandomKitColors(5));

//   const getSpeed = () => {
//     switch (speed) {
//       case "slow":
//         return 0.001;
//       case "fast":
//         return 0.002;
//       default:
//         return 0.001;
//     }
//   };

//   const init = () => {
//     canvas = canvasRef.current;
//     if (!canvas) return;

//     ctx = canvas.getContext("2d");
//     if (!ctx) return;

//     w = ctx.canvas.width = window.innerWidth;
//     h = ctx.canvas.height = window.innerHeight;
//     ctx.filter = `blur(${blur}px)`;
//     nt = 0;

//     const handleResize = () => {
//       if (!ctx) return;
//       w = ctx.canvas.width = window.innerWidth;
//       h = ctx.canvas.height = window.innerHeight;
//       ctx.filter = `blur(${blur}px)`;
//     };

//     window.addEventListener("resize", handleResize);
//     return () => window.removeEventListener("resize", handleResize);
//   };

//   // Use provided colors or fall back to random colors
//   const ribbonColors = colors ?? randomColorsRef.current;

//   const drawRibbon = (n: number) => {
//     if (!ctx) return;

//     nt += getSpeed();

//     for (i = 0; i < n; i++) {
//       ctx.beginPath();
//       ctx.lineWidth = ribbonWidth || 50;
//       ctx.strokeStyle = ribbonColors[i % ribbonColors.length];
//       ctx.globalAlpha = ribbonOpacity;

//       for (x = 0; x < w; x += 5) {
//         const y = (x / 800 + 0.3 * i + nt) * 100;
//         ctx.lineTo(x, y + h * 0.5);
//       }

//       ctx.stroke();
//       ctx.closePath();
//     }
//   };

//   let animationId: number;

//   const render = () => {
//     if (!ctx) return;

//     ctx.clearRect(0, 0, w, h);
//     drawRibbon(5);

//     animationId = requestAnimationFrame(render);
//   };

//   useEffect(() => {
//     const cleanup = init();
//     render();

//     return () => {
//       if (animationId) {
//         cancelAnimationFrame(animationId);
//       }
//       if (cleanup) cleanup();
//     };
//   }, [init, render]);

//   return (
//     <canvas
//       className={kit(
//         "flex absolute inset-0 z-[-1000] h-screen items-center justify-center",
//         className
//       )}
//       ref={canvasRef}
//       style={{
//         filter: `blur(${blur}px)`,
//       }}
//       {...props}
//     />
//   );
// };

// /*##############################################(SNOW)##############################################*/

// interface SnowProps extends SharedProps {
//   color?: string;
//   quantity?: number;
//   speed?: number;
//   maxRadius?: number;
//   minRadius?: number;
// }

// type Snowflake = {
//   x: number;
//   y: number;
//   size: number;
//   alpha: number;
//   dx: number; // Horizontal drift
//   dy: number; // Vertical fall speed
// };

// export const Snow = ({
//   color = "hsl(var(--brand))",
//   quantity = 100,
//   speed = 1,
//   maxRadius = 3,
//   minRadius = 1,
//   className,
//   ...props
// }: SnowProps) => {
//   const canvasRef = useRef<HTMLCanvasElement>(null);
//   const containerRef = useRef<HTMLDivElement>(null);
//   const contextRef = useRef<CanvasRenderingContext2D | null>(null);
//   const snowflakesRef = useRef<Snowflake[]>([]);
//   const animationFrameRef = useRef<number>(0);
//   const [canvasSize, setCanvasSize] = useState({ w: 0, h: 0 });

//   const getPixelRatio = () => {
//     return window.devicePixelRatio || 1;
//   };

//   const getHslColor = useCallback(() => {
//     if (color.startsWith("hsl(var(")) {
//       // Handle CSS variable format: hsl(var(--primary))
//       const element = document.createElement("div");
//       element.style.color = color;
//       document.body.appendChild(element);
//       const computedColor = getComputedStyle(element).color;
//       document.body.removeChild(element);

//       // Convert RGB computed color to HSL
//       const rgb = computedColor.match(/\d+/g)?.map(Number) || [255, 255, 255];
//       const [r, g, b] = rgb.map((v) => v / 255);

//       const max = Math.max(r, g, b);
//       const min = Math.min(r, g, b);
//       const l = (max + min) / 2;
//       let h, s;

//       if (max === min) {
//         h = s = 0;
//       } else {
//         const d = max - min;
//         s = l > 0.5 ? d / (2 - max - min) : d / (max + min);

//         switch (max) {
//           case r:
//             h = (g - b) / d + (g < b ? 6 : 0);
//             break;
//           case g:
//             h = (b - r) / d + 2;
//             break;
//           default:
//             h = (r - g) / d + 4;
//             break;
//         }
//         h *= 60;
//       }

//       return `${Math.round(h)} ${Math.round(s * 100)}% ${Math.round(l * 100)}%`;
//     }

//     // Handle direct HSL format: hsl(220 100% 50%)
//     return color.replace(/hsl\(|\)/g, "");
//   }, [color]);

//   const createSnowflake = useCallback((): Snowflake => {
//     const x = Math.random() * canvasSize.w;
//     const y = Math.random() * canvasSize.h;
//     const size = Math.random() * (maxRadius - minRadius) + minRadius;
//     const alpha = Math.random() * 0.5 + 0.5;
//     const dx = (Math.random() - 0.5) * 0.5;
//     const dy = Math.random() * 0.25 + speed;
//     return { x, y, size, alpha, dx, dy };
//   }, [canvasSize.w, canvasSize.h, maxRadius, minRadius, speed]);

//   const createSnowflakes = useCallback(() => {
//     snowflakesRef.current = Array.from({ length: quantity }, () =>
//       createSnowflake()
//     );
//   }, [quantity, createSnowflake]);

//   const drawSnowflake = useCallback(
//     (snowflake: Snowflake) => {
//       const context = contextRef.current;
//       if (!context) return;

//       const { x, y, size, alpha } = snowflake;
//       context.beginPath();
//       context.arc(x, y, size, 0, Math.PI * 2);
//       context.fillStyle = `hsl(${getHslColor()} / ${alpha})`;
//       context.fill();
//     },
//     [getHslColor]
//   );

//   const animate = useCallback(() => {
//     const context = contextRef.current;
//     if (!context) return;

//     context.clearRect(0, 0, canvasSize.w, canvasSize.h);

//     snowflakesRef.current.forEach((snowflake) => {
//       snowflake.x += snowflake.dx;
//       snowflake.y += snowflake.dy;

//       if (snowflake.y > canvasSize.h) {
//         snowflake.y = -snowflake.size;
//         snowflake.x = Math.random() * canvasSize.w;
//       }

//       drawSnowflake(snowflake);
//     });

//     animationFrameRef.current = requestAnimationFrame(animate);
//   }, [canvasSize.w, canvasSize.h, drawSnowflake]);

//   const resizeCanvas = useCallback(() => {
//     const canvas = canvasRef.current;
//     const container = containerRef.current;
//     const context = contextRef.current;

//     if (!canvas || !container || !context) return;

//     const { offsetWidth, offsetHeight } = container;
//     const pixelRatio = getPixelRatio();

//     setCanvasSize({ w: offsetWidth, h: offsetHeight });

//     canvas.width = offsetWidth * pixelRatio;
//     canvas.height = offsetHeight * pixelRatio;
//     canvas.style.width = `${offsetWidth}px`;
//     canvas.style.height = `${offsetHeight}px`;

//     context.scale(pixelRatio, pixelRatio);
//   }, []);

//   const initCanvas = useCallback(() => {
//     resizeCanvas();
//     createSnowflakes();
//   }, [resizeCanvas, createSnowflakes]);

//   useEffect(() => {
//     const canvas = canvasRef.current;
//     if (canvas) {
//       contextRef.current = canvas.getContext("2d");
//     }

//     initCanvas();
//     animate();

//     const handleResize = () => {
//       initCanvas();
//     };

//     window.addEventListener("resize", handleResize);

//     return () => {
//       window.removeEventListener("resize", handleResize);
//       if (animationFrameRef.current) {
//         cancelAnimationFrame(animationFrameRef.current);
//       }
//     };
//   }, [initCanvas, animate]);

//   return (
//     <div
//       ref={containerRef}
//       className={kit(
//         "flex absolute z-[-1000] h-screen w-screen inset-0",
//         className
//       )}
//       aria-hidden="true"
//       {...props}
//     >
//       <canvas ref={canvasRef} className="absolute inset-0 w-full h-full" />
//     </div>
//   );
// };

// /*##############################################(STARS)##############################################*/

// interface GalacticProps extends SharedProps {
//   color?: string;
//   count?: number;
// }

// type Star = {
//   x: number;
//   y: number;
//   z: number;
//   speed: number;
// };

// export const Galactic = ({
//   color = "hsl(var(--brand))",
//   count = 200,
//   className,
//   ...props
// }: GalacticProps) => {
//   const canvasRef = useRef<HTMLCanvasElement>(null);
//   const animationFrameRef = useRef<number | undefined>(undefined);
//   const galacticRef = useRef<Star[]>([]);

//   const getHslColor = useCallback(() => {
//     if (color.startsWith("hsl(var(")) {
//       // Handle CSS variable format
//       const element = document.createElement("div");
//       element.style.color = color;
//       document.body.appendChild(element);
//       const computedColor = getComputedStyle(element).color;
//       document.body.removeChild(element);

//       // Convert RGB computed color to HSL components
//       const rgb = computedColor.match(/\d+/g)?.map(Number) || [255, 255, 255];
//       return rgb;
//     }

//     // Handle direct HSL values
//     const hslMatch = color.match(/hsl\(([^)]+)\)/);
//     if (hslMatch) {
//       const [h, s, l] = hslMatch[1].split(" ").map((v) => parseFloat(v));
//       // Convert HSL to RGB for canvas
//       const sDecimal = s / 100;
//       const lDecimal = l / 100;

//       const c = (1 - Math.abs(2 * lDecimal - 1)) * sDecimal;
//       const x = c * (1 - Math.abs(((h / 60) % 2) - 1));
//       const m = lDecimal - c / 2;

//       let r, g, b;
//       if (h >= 0 && h < 60) {
//         [r, g, b] = [c, x, 0];
//       } else if (h >= 60 && h < 120) {
//         [r, g, b] = [x, c, 0];
//       } else if (h >= 120 && h < 180) {
//         [r, g, b] = [0, c, x];
//       } else if (h >= 180 && h < 240) {
//         [r, g, b] = [0, x, c];
//       } else if (h >= 240 && h < 300) {
//         [r, g, b] = [x, 0, c];
//       } else {
//         [r, g, b] = [c, 0, x];
//       }

//       return [
//         Math.round((r + m) * 255),
//         Math.round((g + m) * 255),
//         Math.round((b + m) * 255),
//       ];
//     }

//     return [255, 255, 255]; // Default white
//   }, [color]);

//   const createStar = useCallback((canvas: HTMLCanvasElement): Star => {
//     return {
//       x: (Math.random() - 0.5) * 2 * canvas.width,
//       y: (Math.random() - 0.5) * 2 * canvas.height,
//       z: Math.random() * canvas.width,
//       speed: Math.random() * 5 + 2,
//     };
//   }, []);

//   const initGalactic = useCallback(
//     (canvas: HTMLCanvasElement) => {
//       galacticRef.current = Array.from({ length: count }, () =>
//         createStar(canvas)
//       );
//     },
//     [count, createStar]
//   );

//   const drawStar = useCallback(
//     (
//       ctx: CanvasRenderingContext2D,
//       canvas: HTMLCanvasElement,
//       star: Star,
//       perspective: number
//     ) => {
//       const rgb = getHslColor();
//       const scale = perspective / (perspective + star.z);
//       const x2d = canvas.width / 2 + star.x * scale;
//       const y2d = canvas.height / 2 + star.y * scale;
//       const size = Math.max(scale * 3, 0.5);

//       const prevScale = perspective / (perspective + star.z + star.speed * 15);
//       const xPrev = canvas.width / 2 + star.x * prevScale;
//       const yPrev = canvas.height / 2 + star.y * prevScale;

//       // Draw blurred trail
//       ctx.save();
//       ctx.strokeStyle = `rgba(${rgb[0]}, ${rgb[1]}, ${rgb[2]}, 0.2)`;
//       ctx.lineWidth = size * 2.5;
//       ctx.shadowBlur = 35;
//       ctx.shadowColor = `rgba(${rgb[0]}, ${rgb[1]}, ${rgb[2]}, 0.8)`;
//       ctx.beginPath();
//       ctx.moveTo(x2d, y2d);
//       ctx.lineTo(xPrev, yPrev);
//       ctx.stroke();
//       ctx.restore();

//       // Draw sharp line
//       ctx.strokeStyle = `rgba(${rgb[0]}, ${rgb[1]}, ${rgb[2]}, 0.6)`;
//       ctx.lineWidth = size;
//       ctx.beginPath();
//       ctx.moveTo(x2d, y2d);
//       ctx.lineTo(xPrev, yPrev);
//       ctx.stroke();

//       // Draw star point
//       ctx.fillStyle = `rgba(${rgb[0]}, ${rgb[1]}, ${rgb[2]}, 1)`;
//       ctx.beginPath();
//       ctx.arc(x2d, y2d, size / 4, 0, Math.PI * 2);
//       ctx.fill();
//     },
//     [getHslColor]
//   );

//   useEffect(() => {
//     const canvas = canvasRef.current;
//     if (!canvas) return;

//     const ctx = canvas.getContext("2d");
//     if (!ctx) return;

//     const resizeCanvas = () => {
//       canvas.width = canvas.clientWidth;
//       canvas.height = canvas.clientHeight;
//     };

//     const perspective = canvas.width / 2;

//     const animate = () => {
//       ctx.clearRect(0, 0, canvas.width, canvas.height);

//       galacticRef.current.forEach((star) => {
//         drawStar(ctx, canvas, star, perspective);

//         star.z -= star.speed;

//         if (star.z <= 0) {
//           star.z = canvas.width;
//           star.x = (Math.random() - 0.5) * 2 * canvas.width;
//           star.y = (Math.random() - 0.5) * 2 * canvas.height;
//         }
//       });

//       animationFrameRef.current = requestAnimationFrame(animate);
//     };

//     resizeCanvas();
//     initGalactic(canvas);
//     animate();

//     window.addEventListener("resize", resizeCanvas);

//     return () => {
//       window.removeEventListener("resize", resizeCanvas);
//       if (animationFrameRef.current) {
//         cancelAnimationFrame(animationFrameRef.current);
//       }
//     };
//   }, [drawStar, initGalactic]);

//   return (
//     <canvas
//       ref={canvasRef}
//       className={kit("absolute z-[-1000] inset-0 w-full h-full", className)}
//       {...props}
//     />
//   );
// };

// /*##############################################(FLAME)##############################################*/

// interface FlameProps extends SharedProps {
//   color?: string;
//   intensity?: number;
//   flameHeight?: number;
//   speed?: number;
// }

// type Particle = {
//   x: number;
//   y: number;
//   size: number;
//   speedY: number;
//   wiggleX: number;
//   life: number;
//   maxLife: number;
// };

// export const Flame = ({
//   color = "hsl(var(--brand))",
//   intensity = 250,
//   flameHeight = 750,
//   speed = 1,
//   className,
//   ...props
// }: FlameProps) => {
//   const canvasRef = useRef<HTMLCanvasElement>(null);
//   const contextRef = useRef<CanvasRenderingContext2D | null>(null);
//   const particlesRef = useRef<Particle[]>([]);
//   const animationFrameRef = useRef<number>(0);
//   const [canvasSize, setCanvasSize] = useState<{ w: number; h: number }>({
//     w: 0,
//     h: 0,
//   });

//   const getHslColor = useCallback(() => {
//     if (color.startsWith("hsl(var(")) {
//       const element = document.createElement("div");
//       element.style.color = color;
//       document.body.appendChild(element);
//       const computedColor = getComputedStyle(element).color;
//       document.body.removeChild(element);

//       const rgb = computedColor.match(/\d+/g)?.map(Number) || [255, 255, 255];
//       const [r, g, b] = rgb.map((v) => v / 255);

//       const max = Math.max(r, g, b);
//       const min = Math.min(r, g, b);
//       const l = (max + min) / 2;
//       let h, s;

//       if (max === min) {
//         h = s = 0;
//       } else {
//         const d = max - min;
//         s = l > 0.5 ? d / (2 - max - min) : d / (max + min);

//         switch (max) {
//           case r:
//             h = (g - b) / d + (g < b ? 6 : 0);
//             break;
//           case g:
//             h = (b - r) / d + 2;
//             break;
//           default:
//             h = (r - g) / d + 4;
//             break;
//         }
//         h *= 60;
//       }

//       return { h, s: s * 100, l: l * 100 };
//     }

//     // Parse direct HSL color
//     const matches = color.match(/\d+/g)?.map(Number) || [0, 0, 50];
//     return { h: matches[0], s: matches[1], l: matches[2] };
//   }, [color]);

//   const createParticle = useCallback((): Particle => {
//     const canvas = canvasRef.current;
//     if (!canvas) throw new Error("Canvas not initialized");

//     const size = Math.random() * 3 + 2;
//     const centerX = canvas.width / 2;
//     const spreadX = 50;

//     return {
//       x: centerX + (Math.random() - 0.5) * spreadX,
//       y: canvas.height,
//       size,
//       speedY: (Math.random() * 2 + 4) * speed,
//       wiggleX: Math.random() * 4 - 2,
//       life: 0,
//       maxLife: Math.random() * 50 + 50,
//     };
//   }, [speed]);

//   const drawParticle = useCallback(
//     (particle: Particle) => {
//       const context = contextRef.current;
//       if (!context) return;

//       const { h, s, l } = getHslColor();
//       const lifeRatio = 1 - particle.life / particle.maxLife;

//       // Adjust luminance based on particle life
//       const luminance = Math.max(0, l * lifeRatio);
//       // Adjust saturation for more intense core
//       const saturation = Math.min(100, s * (1 + lifeRatio));

//       const gradient = context.createRadialGradient(
//         particle.x,
//         particle.y,
//         0,
//         particle.x,
//         particle.y,
//         particle.size
//       );

//       gradient.addColorStop(
//         0,
//         `hsla(${h}, ${saturation}%, ${luminance}%, ${lifeRatio})`
//       );
//       gradient.addColorStop(1, `hsla(${h}, ${saturation}%, ${luminance}%, 0)`);

//       context.fillStyle = gradient;
//       context.beginPath();
//       context.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);
//       context.fill();
//     },
//     [getHslColor]
//   );

//   const updateParticle = useCallback(
//     (particle: Particle) => {
//       const canvas = canvasRef.current;
//       if (!canvas) return;

//       // Add some wiggle movement
//       particle.x += particle.wiggleX * (1 - particle.life / particle.maxLife);
//       // Particles slow down as they rise
//       particle.y -=
//         particle.speedY * (1 - (particle.life / particle.maxLife) * 0.5);
//       particle.life++;

//       // Reset particle if it's dead or out of bounds
//       if (
//         particle.life >= particle.maxLife ||
//         particle.y < canvas.height - flameHeight
//       ) {
//         const newParticle = createParticle();
//         Object.assign(particle, newParticle);
//       }
//     },
//     [createParticle, flameHeight]
//   );

//   const animate = useCallback(() => {
//     const canvas = canvasRef.current;
//     const context = contextRef.current;
//     if (!canvas || !context) return;

//     context.clearRect(0, 0, canvas.width, canvas.height);

//     // Update and draw particles
//     particlesRef.current.forEach((particle) => {
//       updateParticle(particle);
//       drawParticle(particle);
//     });

//     // Add new particles if needed
//     while (particlesRef.current.length < intensity) {
//       particlesRef.current.push(createParticle());
//     }

//     animationFrameRef.current = requestAnimationFrame(animate);
//   }, [drawParticle, updateParticle, createParticle, intensity]);

//   const resizeCanvas = useCallback(() => {
//     const canvas = canvasRef.current;
//     if (!canvas) return;

//     const context = canvas.getContext("2d");
//     if (!context) return;

//     const dpr = window.devicePixelRatio || 1;
//     const rect = canvas.getBoundingClientRect();

//     setCanvasSize({ w: rect.width, h: rect.height });
//     canvas.width = rect.width * dpr;
//     canvas.height = rect.height * dpr;

//     context.scale(dpr, dpr);
//     contextRef.current = context;
//   }, []);

//   useEffect(() => {
//     resizeCanvas();
//     particlesRef.current = Array.from({ length: intensity }, createParticle);
//     animate();

//     window.addEventListener("resize", resizeCanvas);

//     return () => {
//       window.removeEventListener("resize", resizeCanvas);
//       if (animationFrameRef.current) {
//         cancelAnimationFrame(animationFrameRef.current);
//       }
//     };
//   }, [resizeCanvas, animate, createParticle, intensity]);

//   return (
//     <canvas
//       ref={canvasRef}
//       className={kit("absolute z-[-1000] inset-0 w-full h-full", className)}
//       {...props}
//     />
//   );
// };

// /*##############################################(FIREWORKS)##############################################*/

// interface FireworksProps extends SharedProps {
//   frequency?: number; // How often to launch new fireworks
//   intensity?: number; // Number of particles per explosion
//   gravity?: number;
//   friction?: number;
// }

// type FireworksParticle = {
//   x: number;
//   y: number;
//   vx: number;
//   vy: number;
//   life: number;
//   maxLife: number;
//   color: string;
//   size: number;
// };

// type FireworksRocket = {
//   x: number;
//   y: number;
//   targetY: number;
//   vy: number;
//   color: string;
// };

// export const Fireworks = ({
//   frequency = 100,
//   intensity = 50,
//   gravity = 0.2,
//   friction = 0.99,
//   className,
//   ...props
// }: FireworksProps) => {
//   const canvasRef = useRef<HTMLCanvasElement>(null);
//   const contextRef = useRef<CanvasRenderingContext2D | null>(null);
//   const rocketsRef = useRef<FireworksRocket[]>([]);
//   const particlesRef = useRef<FireworksParticle[]>([]);
//   const animationFrameRef = useRef<number>(0);
//   const lastLaunchRef = useRef(0);

//   const getHslValues = useCallback((colorStr: string) => {
//     if (colorStr.startsWith("hsl(var(")) {
//       const element = document.createElement("div");
//       element.style.color = colorStr;
//       document.body.appendChild(element);
//       const computedColor = getComputedStyle(element).color;
//       document.body.removeChild(element);

//       const rgb = computedColor.match(/\d+/g)?.map(Number) || [255, 255, 255];
//       const [r, g, b] = rgb.map((v) => v / 255);

//       const max = Math.max(r, g, b);
//       const min = Math.min(r, g, b);
//       const l = (max + min) / 2;
//       let h, s;

//       if (max === min) {
//         h = s = 0;
//       } else {
//         const d = max - min;
//         s = l > 0.5 ? d / (2 - max - min) : d / (max + min);

//         switch (max) {
//           case r:
//             h = (g - b) / d + (g < b ? 6 : 0);
//             break;
//           case g:
//             h = (b - r) / d + 2;
//             break;
//           default:
//             h = (r - g) / d + 4;
//             break;
//         }
//         h *= 60;
//       }

//       return { h, s: s * 100, l: l * 100 };
//     }

//     return { h: 0, s: 100, l: 50 }; // Default fallback
//   }, []);

//   const createRocket = useCallback(
//     (canvas: HTMLCanvasElement): FireworksRocket => {
//       const color =
//         KitStaticColors[Math.floor(Math.random() * KitStaticColors.length)];

//       return {
//         x: Math.random() * canvas.width,
//         y: canvas.height,
//         targetY: canvas.height * 0.2 + Math.random() * (canvas.height * 0.5),
//         vy: -15 - Math.random() * 5,
//         color,
//       };
//     },
//     []
//   );

//   const createExplosionParticles = useCallback(
//     (x: number, y: number, color: string) => {
//       const particles: FireworksParticle[] = [];

//       // Convert CSS variable to actual color
//       let finalColor = color;
//       if (color.startsWith("var")) {
//         const element = document.createElement("div");
//         element.style.color = `hsl(${color})`;
//         document.body.appendChild(element);
//         finalColor = getComputedStyle(element).color;
//         document.body.removeChild(element);
//       }

//       for (let i = 0; i < intensity; i++) {
//         const angle = (Math.PI * 2 * i) / intensity;
//         const speed = 8 + Math.random() * 4;

//         // For direct hex colors, use them as is
//         if (finalColor.startsWith("#")) {
//           particles.push({
//             x,
//             y,
//             vx: Math.cos(angle) * speed * (0.8 + Math.random() * 0.4),
//             vy: Math.sin(angle) * speed * (0.8 + Math.random() * 0.4),
//             life: 0,
//             maxLife: 80 + Math.random() * 40,
//             color: finalColor,
//             size: 2 + Math.random() * 2,
//           });
//         } else {
//           // For RGB colors from CSS variables, convert to HSL for better opacity handling
//           const rgb = finalColor.match(/\d+/g)?.map(Number) || [255, 0, 0];
//           const [r, g, b] = rgb.map((v) => v / 255);

//           const max = Math.max(r, g, b);
//           const min = Math.min(r, g, b);
//           const l = (max + min) / 2;
//           let h, s;

//           if (max === min) {
//             h = s = 0;
//           } else {
//             const d = max - min;
//             s = l > 0.5 ? d / (2 - max - min) : d / (max + min);

//             switch (max) {
//               case r:
//                 h = (g - b) / d + (g < b ? 6 : 0);
//                 break;
//               case g:
//                 h = (b - r) / d + 2;
//                 break;
//               default:
//                 h = (r - g) / d + 4;
//                 break;
//             }
//             h *= 60;
//           }

//           particles.push({
//             x,
//             y,
//             vx: Math.cos(angle) * speed * (0.8 + Math.random() * 0.4),
//             vy: Math.sin(angle) * speed * (0.8 + Math.random() * 0.4),
//             life: 0,
//             maxLife: 80 + Math.random() * 40,
//             color: `hsl(${h}, ${s * 100}%, ${l * 100}%)`,
//             size: 2 + Math.random() * 2,
//           });
//         }
//       }

//       return particles;
//     },
//     [intensity]
//   );

//   const drawParticle = useCallback(
//     (ctx: CanvasRenderingContext2D, particle: FireworksParticle) => {
//       const opacity = 1 - particle.life / particle.maxLife;

//       if (particle.color.startsWith("#")) {
//         // Convert hex to rgba
//         const r = parseInt(particle.color.slice(1, 3), 16);
//         const g = parseInt(particle.color.slice(3, 5), 16);
//         const b = parseInt(particle.color.slice(5, 7), 16);
//         ctx.fillStyle = `rgba(${r}, ${g}, ${b}, ${opacity})`;
//       } else if (particle.color.startsWith("hsl")) {
//         ctx.fillStyle = particle.color
//           .replace("hsl", "hsla")
//           .replace(")", `, ${opacity})`);
//       } else {
//         // Handle RGB colors
//         ctx.fillStyle = particle.color
//           .replace("rgb", "rgba")
//           .replace(")", `, ${opacity})`);
//       }

//       ctx.beginPath();
//       ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);
//       ctx.fill();
//     },
//     []
//   );

//   const drawRocket = useCallback(
//     (ctx: CanvasRenderingContext2D, rocket: FireworksRocket) => {
//       const gradient = ctx.createLinearGradient(
//         rocket.x,
//         rocket.y + 10,
//         rocket.x,
//         rocket.y - 10
//       );
//       gradient.addColorStop(0, "hsla(45, 100%, 50%, 0)");
//       gradient.addColorStop(1, "hsla(45, 100%, 50%, 1)");

//       ctx.fillStyle = gradient;
//       ctx.beginPath();
//       ctx.arc(rocket.x, rocket.y, 2, 0, Math.PI * 2);
//       ctx.fill();
//     },
//     []
//   );

//   const animate = useCallback(() => {
//     const canvas = canvasRef.current;
//     const ctx = contextRef.current;
//     if (!canvas || !ctx) return;

//     // Only clear the rectangle, don't fill it
//     ctx.clearRect(0, 0, canvas.width, canvas.height);

//     // Launch new rockets
//     const now = Date.now();
//     if (now - lastLaunchRef.current > frequency) {
//       rocketsRef.current.push(createRocket(canvas));
//       lastLaunchRef.current = now;
//     }

//     // Update and draw rockets
//     rocketsRef.current = rocketsRef.current.filter((rocket) => {
//       rocket.y += rocket.vy;
//       drawRocket(ctx, rocket);

//       // Explode if reached target height
//       if (rocket.y <= rocket.targetY) {
//         particlesRef.current.push(
//           ...createExplosionParticles(rocket.x, rocket.y, rocket.color)
//         );
//         return false;
//       }
//       return true;
//     });

//     // Update and draw particles
//     particlesRef.current = particlesRef.current.filter((particle) => {
//       particle.x += particle.vx;
//       particle.y += particle.vy;
//       particle.vy += gravity;
//       particle.vx *= friction;
//       particle.vy *= friction;
//       particle.life++;

//       drawParticle(ctx, particle);

//       return particle.life < particle.maxLife;
//     });

//     animationFrameRef.current = requestAnimationFrame(animate);
//   }, [
//     frequency,
//     gravity,
//     friction,
//     createRocket,
//     createExplosionParticles,
//     drawRocket,
//     drawParticle,
//   ]);
//   const resizeCanvas = useCallback(() => {
//     const canvas = canvasRef.current;
//     if (!canvas) return;

//     const ctx = canvas.getContext("2d", { alpha: true });
//     if (!ctx) return;

//     const dpr = window.devicePixelRatio || 1;
//     const rect = canvas.getBoundingClientRect();

//     canvas.width = rect.width * dpr;
//     canvas.height = rect.height * dpr;

//     ctx.scale(dpr, dpr);
//     contextRef.current = ctx;
//   }, []);

//   useEffect(() => {
//     resizeCanvas();
//     animate();

//     window.addEventListener("resize", resizeCanvas);

//     return () => {
//       window.removeEventListener("resize", resizeCanvas);
//       if (animationFrameRef.current) {
//         cancelAnimationFrame(animationFrameRef.current);
//       }
//     };
//   }, [resizeCanvas, animate]);

//   return (
//     <canvas
//       ref={canvasRef}
//       className={kit(
//         "flex absolute z-[-1000] inset-0 w-full h-full",
//         className
//       )}
//       {...props}
//     />
//   );
// };

// /*##############################################(FLICKER)##############################################*/

// interface FlickerProps {
//   squareSize?: number;
//   gridGap?: number;
//   flickerChance?: number;
//   color?: string;
//   width?: number;
//   height?: number;
//   className?: string;
//   maxOpacity?: number;
// }

// type Square = {
//   opacity: number;
//   targetOpacity: number;
//   transitionSpeed: number;
// };

// export const Flicker: React.FC<FlickerProps> = ({
//   squareSize = 4,
//   gridGap = 6,
//   flickerChance = 0.8,
//   color = "hsl(var(--primary))",
//   width,
//   height,
//   className,
//   maxOpacity = 0.2,
// }) => {
//   const canvasRef = useRef<HTMLCanvasElement>(null);
//   const [isInView, setIsInView] = useState(false);

//   const getHSLValues = useCallback((colorStr: string) => {
//     // Handle HSL variables (e.g., "hsl(var(--brand))")
//     if (colorStr.includes("var(")) {
//       const element = document.createElement("div");
//       element.style.color = colorStr;
//       document.body.appendChild(element);
//       const computedColor = getComputedStyle(element).color;
//       document.body.removeChild(element);

//       // Parse RGB values
//       const rgb = computedColor.match(/\d+/g)?.map(Number) || [0, 0, 0];
//       const [r, g, b] = rgb.map((v) => v / 255);

//       const max = Math.max(r, g, b);
//       const min = Math.min(r, g, b);
//       const l = (max + min) / 2;
//       let h, s;

//       if (max === min) {
//         h = s = 0;
//       } else {
//         const d = max - min;
//         s = l > 0.5 ? d / (2 - max - min) : d / (max + min);

//         switch (max) {
//           case r:
//             h = (g - b) / d + (g < b ? 6 : 0);
//             break;
//           case g:
//             h = (b - r) / d + 2;
//             break;
//           default:
//             h = (r - g) / d + 4;
//             break;
//         }
//         h *= 60;
//       }

//       return { h, s: s * 100, l: l * 100 };
//     }

//     // Handle Tailwind HSL variable directly (e.g., "var(--brand)")
//     if (colorStr.startsWith("var(")) {
//       const variableValue = getComputedStyle(document.documentElement)
//         .getPropertyValue(colorStr.slice(4, -1))
//         .trim();

//       // Parse the HSL values directly
//       const [h, s, l] = variableValue.split(" ").map(Number);
//       return { h, s, l };
//     }

//     // Already HSL format
//     if (colorStr.startsWith("hsl")) {
//       const values = colorStr.match(/\d+/g)?.map(Number) || [0, 0, 0];
//       return { h: values[0], s: values[1], l: values[2] };
//     }

//     // Fallback
//     return { h: 0, s: 0, l: 0 };
//   }, []);

//   const setupCanvas = useCallback(
//     (canvas: HTMLCanvasElement) => {
//       const canvasWidth = width || canvas.clientWidth;
//       const canvasHeight = height || canvas.clientHeight;
//       const dpr = window.devicePixelRatio || 1;

//       canvas.width = canvasWidth * dpr;
//       canvas.height = canvasHeight * dpr;
//       canvas.style.width = `${canvasWidth}px`;
//       canvas.style.height = `${canvasHeight}px`;

//       const cols = Math.floor(canvasWidth / (squareSize + gridGap));
//       const rows = Math.floor(canvasHeight / (squareSize + gridGap));

//       const squares: Square[] = new Array(cols * rows).fill(null).map(() => ({
//         opacity: Math.random() * maxOpacity,
//         targetOpacity: Math.random() * maxOpacity,
//         transitionSpeed: 0.1 + Math.random() * 0.4,
//       }));

//       return {
//         width: canvasWidth,
//         height: canvasHeight,
//         cols,
//         rows,
//         squares,
//         dpr,
//       };
//     },
//     [squareSize, gridGap, width, height, maxOpacity]
//   );

//   const updateSquares = useCallback(
//     (squares: Square[], deltaTime: number) => {
//       squares.forEach((square, i) => {
//         if (Math.random() < flickerChance * deltaTime) {
//           square.targetOpacity = Math.random() * maxOpacity;
//           square.transitionSpeed = 0.1 + Math.random() * 0.4;
//         }

//         // Smooth transition to target opacity
//         const diff = square.targetOpacity - square.opacity;
//         square.opacity += diff * square.transitionSpeed * deltaTime;
//       });
//     },
//     [flickerChance, maxOpacity]
//   );

//   const drawGrid = useCallback(
//     (
//       ctx: CanvasRenderingContext2D,
//       width: number,
//       height: number,
//       cols: number,
//       rows: number,
//       squares: Square[],
//       dpr: number
//     ) => {
//       ctx.clearRect(0, 0, width, height);

//       const hsl = getHSLValues(color);

//       for (let i = 0; i < cols; i++) {
//         for (let j = 0; j < rows; j++) {
//           const square = squares[i * rows + j];
//           // Use the HSL values directly without additional processing
//           ctx.fillStyle = `hsla(${hsl.h} ${hsl.s}% ${hsl.l}% / ${square.opacity})`;
//           ctx.fillRect(
//             i * (squareSize + gridGap) * dpr,
//             j * (squareSize + gridGap) * dpr,
//             squareSize * dpr,
//             squareSize * dpr
//           );
//         }
//       }
//     },
//     [color, squareSize, gridGap, getHSLValues]
//   );

//   useEffect(() => {
//     const canvas = canvasRef.current;
//     if (!canvas) return;

//     const ctx = canvas.getContext("2d", { alpha: true });
//     if (!ctx) return;

//     let animationFrameId: number;
//     let { width, height, cols, rows, squares, dpr } = setupCanvas(canvas);

//     let lastTime = 0;
//     const animate = (time: number) => {
//       if (!isInView) return;

//       const deltaTime = (time - lastTime) / 1000;
//       lastTime = time;

//       updateSquares(squares, deltaTime);
//       drawGrid(ctx, width * dpr, height * dpr, cols, rows, squares, dpr);
//       animationFrameId = requestAnimationFrame(animate);
//     };

//     const handleResize = () => {
//       ({ width, height, cols, rows, squares, dpr } = setupCanvas(canvas));
//     };

//     const observer = new IntersectionObserver(
//       ([entry]) => {
//         setIsInView(entry.isIntersecting);
//       },
//       { threshold: 0 }
//     );

//     observer.observe(canvas);
//     window.addEventListener("resize", handleResize);

//     if (isInView) {
//       animationFrameId = requestAnimationFrame(animate);
//     }

//     return () => {
//       window.removeEventListener("resize", handleResize);
//       cancelAnimationFrame(animationFrameId);
//       observer.disconnect();
//     };
//   }, [setupCanvas, updateSquares, drawGrid, width, height, isInView]);

//   return (
//     <canvas
//       ref={canvasRef}
//       className={kit(
//         "flex absolute z-[-1000] inset-0 w-full h-full shadow-hollow",
//         className
//       )}
//     />
//   );
// };

// /*##############################################(AURORA)##############################################*/

// interface AuroraProps extends SharedProps {
//   showRadialGradient?: boolean;
//   intensity?: number;
//   speed?: number;
// }

// interface Wave {
//   baseHue: number;
//   amplitude: number;
//   frequency: number;
//   phase: number;
//   speed: number;
//   opacity: number;
// }

// export const Aurora: React.FC<AuroraProps> = ({
//   showRadialGradient = true,
//   intensity = 0.5,
//   speed = 1,
//   className,
//   ...props
// }) => {
//   const canvasRef = useRef<HTMLCanvasElement>(null);
//   const isDark = useRef(false);

//   const createWaves = useCallback(() => {
//     return [
//       {
//         baseHue: 230,
//         amplitude: 50,
//         frequency: 0.005,
//         phase: 0,
//         speed: 0.5 * speed,
//         opacity: 0.5 * intensity,
//       },
//       {
//         baseHue: 250,
//         amplitude: 40,
//         frequency: 0.008,
//         phase: 2,
//         speed: 0.4 * speed,
//         opacity: 0.4 * intensity,
//       },
//       {
//         baseHue: 270,
//         amplitude: 45,
//         frequency: 0.006,
//         phase: 4,
//         speed: 0.3 * speed,
//         opacity: 0.3 * intensity,
//       },
//     ];
//   }, [intensity, speed]);

//   const drawWave = useCallback(
//     (
//       ctx: CanvasRenderingContext2D,
//       wave: Wave,
//       width: number,
//       height: number,
//       time: number
//     ) => {
//       // Create gradient from top to bottom
//       const gradient = ctx.createLinearGradient(0, 0, 0, height);

//       // Create smooth gradient points
//       for (let i = 0; i <= 1; i += 0.2) {
//         const hue = wave.baseHue + Math.sin(time * 0.2 + i * Math.PI) * 15;
//         const saturation = 70 + Math.sin(time * 0.1 + i * Math.PI) * 10;
//         const lightness = isDark.current ? 40 : 60;
//         const alpha = wave.opacity * (1 - i * 0.5); // Fade out towards bottom

//         gradient.addColorStop(
//           i,
//           `hsla(${hue}, ${saturation}%, ${lightness}%, ${alpha})`
//         );
//       }

//       ctx.fillStyle = gradient;
//       ctx.beginPath();
//       ctx.moveTo(0, height);

//       // Draw smooth wave
//       for (let x = 0; x <= width; x++) {
//         const dx = x * wave.frequency;
//         const y =
//           height * 0.5 + // Center vertically
//           Math.sin(dx + wave.phase + time * wave.speed) * wave.amplitude +
//           Math.sin(dx * 0.5 + time * wave.speed * 0.5) * wave.amplitude * 0.3;

//         if (x === 0) {
//           ctx.moveTo(x, y);
//         } else {
//           ctx.lineTo(x, y);
//         }
//       }

//       ctx.lineTo(width, height);
//       ctx.lineTo(0, height);
//       ctx.closePath();
//       ctx.fill();
//     },
//     []
//   );

//   const createRadialMask = useCallback(
//     (ctx: CanvasRenderingContext2D, width: number, height: number) => {
//       const gradient = ctx.createRadialGradient(
//         width * 0.8,
//         height * 0.2,
//         0, // Inner circle position
//         width * 0.8,
//         height * 0.2,
//         height // Outer circle position
//       );

//       gradient.addColorStop(0, "rgba(0, 0, 0, 1)");
//       gradient.addColorStop(0.5, "rgba(0, 0, 0, 0.5)");
//       gradient.addColorStop(1, "rgba(0, 0, 0, 0)");

//       return gradient;
//     },
//     []
//   );

//   useEffect(() => {
//     const canvas = canvasRef.current;
//     if (!canvas) return;

//     const ctx = canvas.getContext("2d", { alpha: true });
//     if (!ctx) return;

//     const waves = createWaves();
//     let animationFrame: number;
//     let lastTime = 0;

//     const resizeCanvas = () => {
//       const rect = canvas.getBoundingClientRect();
//       const dpr = window.devicePixelRatio || 1;

//       canvas.width = rect.width * dpr;
//       canvas.height = rect.height * dpr;

//       ctx.scale(dpr, dpr);
//       canvas.style.width = `${rect.width}px`;
//       canvas.style.height = `${rect.height}px`;
//     };

//     const animate = (timestamp: number) => {
//       if (!canvas || !ctx) return;

//       const deltaTime = timestamp - lastTime;
//       lastTime = timestamp;

//       ctx.clearRect(0, 0, canvas.width, canvas.height);

//       // Update dark mode
//       isDark.current = window.matchMedia(
//         "(prefers-color-scheme: dark)"
//       ).matches;

//       // Draw waves from back to front
//       waves.forEach((wave) => {
//         drawWave(ctx, wave, canvas.width, canvas.height, timestamp * 0.001);
//       });

//       // Apply radial gradient mask
//       if (showRadialGradient) {
//         ctx.globalCompositeOperation = "destination-in";
//         ctx.fillStyle = createRadialMask(ctx, canvas.width, canvas.height);
//         ctx.fillRect(0, 0, canvas.width, canvas.height);
//         ctx.globalCompositeOperation = "source-over";
//       }

//       animationFrame = requestAnimationFrame(animate);
//     };

//     // Setup resize observer
//     const resizeObserver = new ResizeObserver(() => {
//       resizeCanvas();
//     });
//     resizeObserver.observe(canvas);

//     // Initial setup
//     resizeCanvas();
//     animationFrame = requestAnimationFrame(animate);

//     return () => {
//       cancelAnimationFrame(animationFrame);
//       resizeObserver.disconnect();
//     };
//   }, [createWaves, drawWave, createRadialMask, showRadialGradient]);

//   return (
//     <canvas
//       ref={canvasRef}
//       className="flex absolute z-[-1000] inset-0 w-full h-full"
//       style={{ opacity: 0.5 }}
//     />
//   );
// };

// /*##############################################(SPOTLIGHT)##############################################*/
// interface SpotlightProps extends SharedProps {
//   fill?: string;
//   intensity?: number;
//   size?: number;
//   blur?: number;
// }

// export const Spotlight: React.FC<SpotlightProps> = ({
//   className,
//   fill = "255, 255, 255",
//   intensity = 0.21,
//   size = 0.8,
//   blur = 150,
// }) => {
//   const canvasRef = useRef<HTMLCanvasElement>(null);
//   const progressRef = useRef(0);
//   const startTimeRef = useRef<number | null>(null);
//   const animationDuration = 2000; // 2 seconds
//   const animationDelay = 750; // 0.75 seconds

//   const drawSpotlight = useCallback(
//     (
//       ctx: CanvasRenderingContext2D,
//       width: number,
//       height: number,
//       time: number,
//       progress: number
//     ) => {
//       ctx.clearRect(0, 0, width, height);

//       // Calculate animation values
//       const scale = 0.5 + progress * 0.5; // Scale from 0.5 to 1
//       const currentOpacity = progress * intensity;
//       const translateX = -72 + progress * 22; // -72% to -50%
//       const translateY = -62 + progress * 22; // -62% to -40%

//       // Apply transforms
//       ctx.save();
//       ctx.translate(width * (translateX / 100), height * (translateY / 100));
//       ctx.scale(scale, scale);

//       // Calculate spotlight position
//       const centerX = width * 0.5;
//       const startY = -height * 0.2;

//       // Create cone shape
//       const topWidth = width * 0.05;
//       const bottomWidth = width * 0.8;
//       const coneHeight = height * 1.2;

//       // Add slight movement
//       const wobble = Math.sin(time * 0.001) * width * 0.02;

//       // Create main gradient for the cone
//       const gradient = ctx.createLinearGradient(
//         centerX,
//         startY,
//         centerX,
//         startY + coneHeight
//       );

//       gradient.addColorStop(0, `rgba(${fill}, ${currentOpacity * 0.8})`);
//       gradient.addColorStop(0.2, `rgba(${fill}, ${currentOpacity * 0.6})`);
//       gradient.addColorStop(0.6, `rgba(${fill}, ${currentOpacity * 0.3})`);
//       gradient.addColorStop(1, `rgba(${fill}, 0)`);

//       // Draw cone shape
//       ctx.beginPath();
//       ctx.moveTo(centerX - topWidth + wobble, startY);
//       ctx.lineTo(centerX + topWidth + wobble, startY);

//       ctx.quadraticCurveTo(
//         centerX + bottomWidth * 0.7 + wobble,
//         startY + coneHeight * 0.5,
//         centerX + bottomWidth * 0.5,
//         startY + coneHeight
//       );
//       ctx.quadraticCurveTo(
//         centerX - bottomWidth * 0.7 + wobble,
//         startY + coneHeight * 0.5,
//         centerX - topWidth + wobble,
//         startY
//       );

//       ctx.fillStyle = gradient;
//       ctx.filter = `blur(${blur}px)`;
//       ctx.fill();

//       // Add inner glow with current opacity
//       const innerGradient = ctx.createLinearGradient(
//         centerX,
//         startY,
//         centerX,
//         startY + coneHeight * 0.5
//       );

//       innerGradient.addColorStop(0, `rgba(${fill}, ${currentOpacity * 0.5})`);
//       innerGradient.addColorStop(1, `rgba(${fill}, 0)`);

//       ctx.fillStyle = innerGradient;
//       ctx.filter = `blur(${blur * 0.5}px)`;

//       // Draw inner cone
//       ctx.beginPath();
//       const innerTopWidth = topWidth * 0.5;
//       const innerBottomWidth = bottomWidth * 0.3;

//       ctx.moveTo(centerX - innerTopWidth + wobble, startY);
//       ctx.lineTo(centerX + innerTopWidth + wobble, startY);
//       ctx.quadraticCurveTo(
//         centerX + innerBottomWidth + wobble,
//         startY + coneHeight * 0.3,
//         centerX,
//         startY + coneHeight * 0.5
//       );
//       ctx.quadraticCurveTo(
//         centerX - innerBottomWidth + wobble,
//         startY + coneHeight * 0.3,
//         centerX - innerTopWidth + wobble,
//         startY
//       );

//       ctx.fill();
//       ctx.restore();
//     },
//     [fill, intensity, blur]
//   );

//   useEffect(() => {
//     const canvas = canvasRef.current;
//     if (!canvas) return;

//     const ctx = canvas.getContext("2d", { alpha: true });
//     if (!ctx) return;

//     let animationFrame: number;

//     const resizeCanvas = () => {
//       const rect = canvas.getBoundingClientRect();
//       const dpr = window.devicePixelRatio || 1;

//       canvas.width = rect.width * dpr;
//       canvas.height = rect.height * dpr;

//       ctx.scale(dpr, dpr);
//       canvas.style.width = `${rect.width}px`;
//       canvas.style.height = `${rect.height}px`;
//     };

//     const animate = (timestamp: number) => {
//       if (startTimeRef.current === null) {
//         startTimeRef.current = timestamp + animationDelay;
//       }

//       const elapsed = timestamp - startTimeRef.current;
//       progressRef.current = Math.min(
//         1,
//         Math.max(0, elapsed / animationDuration)
//       );

//       drawSpotlight(
//         ctx,
//         canvas.width,
//         canvas.height,
//         timestamp,
//         progressRef.current
//       );

//       if (progressRef.current < 1 || elapsed > 0) {
//         animationFrame = requestAnimationFrame(animate);
//       }
//     };

//     // Setup resize observer
//     const resizeObserver = new ResizeObserver(resizeCanvas);
//     resizeObserver.observe(canvas);

//     // Initial setup
//     resizeCanvas();
//     animationFrame = requestAnimationFrame(animate);

//     return () => {
//       cancelAnimationFrame(animationFrame);
//       resizeObserver.disconnect();
//     };
//   }, [drawSpotlight]);

//   return (
//     <canvas
//       ref={canvasRef}
//       className={kit(
//         "flex absolute z-[-1000] h-[180%] w-[138%] lg:w-[84%]",
//         className
//       )}
//     />
//   );
// };

// /*##############################################(ERROR BOUNDARY)##############################################*/
// // Pattern Error Boundary Component
// class PatternErrorBoundary extends React.Component<
//   {
//     children: React.ReactNode;
//     patternName: string;
//     onError: (error: Error, patternName: string) => void;
//     fallback?: React.ReactNode;
//   },
//   { hasError: boolean }
// > {
//   constructor(props: any) {
//     super(props);
//     this.state = { hasError: false };
//   }

//   static getDerivedStateFromError() {
//     return { hasError: true };
//   }

//   componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
//     console.error(`Error in pattern "${this.props.patternName}":`, error);
//     this.props.onError(error, this.props.patternName);
//   }

//   render() {
//     if (this.state.hasError) {
//       return (
//         this.props.fallback || (
//           <div className="flex items-center justify-center h-full w-full bg-red-50 bg-opacity-20 text-red-500">
//             <div className="p-4 text-center">
//               <p className="font-semibold">
//                 Pattern Error: {this.props.patternName}
//               </p>
//               <p className="text-sm opacity-75">Using fallback pattern</p>
//             </div>
//           </div>
//         )
//       );
//     }

//     return this.props.children;
//   }
// }

// /*##############################################(SAFE PATTERN WRAPPER - HOD)##############################################*/
// // Safe Pattern Wrapper Higher Order Component
// function withErrorHandling<P extends object>(
//   Component: React.ComponentType<P>,
//   patternName: string,
//   onError: (error: Error, patternName: string) => void
// ) {
//   return function SafePatternComponent(props: P) {
//     return (
//       <PatternErrorBoundary patternName={patternName} onError={onError}>
//         <Component {...props} />
//       </PatternErrorBoundary>
//     );
//   };
// }

// /*##############################################(PATTERN)##############################################*/

// // Enhanced Pattern Component with Error Handling and Debugging
// export default function Pattern({
//   variant = "tile",
//   format = "base",
//   intensity,
//   speed,
//   color,
//   blur,
//   animate = "none",
//   randomize = false,
//   randomInterval = 5000,
//   className,
//   children,
//   debug = false,
//   ...props
// }: PatternProps & {
//   randomize?: boolean;
//   randomInterval?: number;
//   debug?: boolean;
// }) {
//   // Track error state
//   const [errorState, setErrorState] = useState<{
//     hasError: boolean;
//     patternName?: string;
//     errorMessage?: string;
//   }>({ hasError: false });

//   // Track enabled/disabled patterns for debugging
//   const [disabledPatterns, setDisabledPatterns] = useState<Set<PatternVariant>>(
//     new Set()
//   );

//   // Store current pattern for randomization or fallback handling
//   const [currentPattern, setCurrentPattern] = useState<PatternVariant>(variant);
//   const [previousValidPattern, setPreviousValidPattern] =
//     useState<PatternVariant>("tile");

//   // Handle pattern errors
//   const handlePatternError = useCallback(
//     (error: Error, patternName: PatternVariant) => {
//       console.warn(
//         `Pattern "${patternName}" threw an error. Disabling it.`,
//         error
//       );
//       setErrorState({
//         hasError: true,
//         patternName: patternName,
//         errorMessage: error.message,
//       });

//       setDisabledPatterns((prev) => {
//         const updated = new Set(prev);
//         updated.add(patternName as PatternVariant);
//         return updated;
//       });

//       // Fall back to previous valid pattern or tile
//       if (randomize) {
//         changeToNextValidPattern();
//       } else {
//         setCurrentPattern(previousValidPattern);
//       }
//     },
//     [randomize, previousValidPattern]
//   );

//   // Cache and get available patterns to avoid recreating the array on every render excluding disabled ones
//   const availablePatterns = useMemo(() => {
//     return Object.keys(PatternVariants.variants.variant).filter(
//       (p) => !disabledPatterns.has(p as PatternVariant)
//     ) as PatternVariant[];
//   }, [disabledPatterns]);

//   // Shuffle patterns for randomization
//   const shuffledPatterns = useMemo(
//     () => shufflePatterns(availablePatterns),
//     [availablePatterns]
//   );

//   // Track current index in shuffled patterns
//   const [currentIndex, setCurrentIndex] = useState(0);

//   // Function to change to next valid pattern
//   const changeToNextValidPattern = useCallback(() => {
//     if (availablePatterns.length === 0) return;

//     setCurrentIndex((prevIndex) => {
//       const nextIndex = (prevIndex + 1) % shuffledPatterns.length;
//       const nextPattern = shuffledPatterns[nextIndex];
//       setCurrentPattern(nextPattern);
//       setPreviousValidPattern(nextPattern);
//       return nextIndex;
//     });
//   }, [availablePatterns, shuffledPatterns]);

//   // Main pattern change function with debouncing
//   const changePattern = useCallback(() => {
//     if (!randomize || availablePatterns.length === 0) return;
//     changeToNextValidPattern();
//   }, [randomize, availablePatterns.length, changeToNextValidPattern]);

//   // Debounced version of pattern change
//   const debouncedChangePattern = useMemo(
//     () => debounce(changePattern, 100),
//     [changePattern]
//   );

//   // Set up pattern rotation interval
//   useEffect(() => {
//     // Set initial pattern based on prop or fallback
//     if (!randomize) {
//       if (disabledPatterns.has(variant)) {
//         setCurrentPattern(previousValidPattern);
//       } else {
//         setCurrentPattern(variant);
//         setPreviousValidPattern(variant);
//       }
//       return;
//     }

//     // Handle randomization
//     const intervalId = setInterval(debouncedChangePattern, randomInterval);
//     return () => clearInterval(intervalId);
//   }, [
//     randomize,
//     randomInterval,
//     variant,
//     debouncedChangePattern,
//     disabledPatterns,
//     previousValidPattern,
//   ]);

//   // Create wrapped pattern components with error boundaries
//   const SafePatterns = useMemo(() => {
//     // @ts-ignore
//     const components: Record<PatternVariant, React.ComponentType<any>> = {
//       ripple: withErrorHandling(Ripple, "ripple", handlePatternError as any),
//       tile: withErrorHandling(Tile, "tile", handlePatternError as any),
//       beam: withErrorHandling(Beam, "beam", handlePatternError as any),
//       rain: withErrorHandling(Rain, "rain", handlePatternError as any),
//       ribbon: withErrorHandling(Ribbon, "ribbon", handlePatternError as any),
//       snow: withErrorHandling(Snow, "snow", handlePatternError as any),
//       galactic: withErrorHandling(
//         Galactic,
//         "galactic",
//         handlePatternError as any
//       ),
//       flame: withErrorHandling(Flame, "flame", handlePatternError as any),
//       fireworks: withErrorHandling(
//         Fireworks,
//         "fireworks",
//         handlePatternError as any
//       ),
//       aurora: withErrorHandling(Aurora, "aurora", handlePatternError as any),
//       "confetti-trail": withErrorHandling(
//         ConfettiTrail,
//         "confetti-trail",
//         handlePatternError as any
//       ),
//       sparkle: withErrorHandling(Sparkle, "sparkle", handlePatternError as any),
//       flicker: withErrorHandling(Flicker, "flicker", handlePatternError as any),
//       meteor: withErrorHandling(Meteor, "meteor", handlePatternError as any),
//       spotlight: withErrorHandling(
//         Spotlight,
//         "spotlight",
//         handlePatternError as any
//       ),
//       vortex: withErrorHandling(Vortex, "vortex", handlePatternError as any),
//       // gradial: undefined,
//       // none: undefined,
//       // grid: undefined,
//       // dot: undefined
//     };

//     return components;
//   }, [handlePatternError]);

//   // Render the selected pattern with error handling
//   const renderPattern = useCallback(() => {
//     if (currentPattern === "none") return null;
//     const SafePattern = SafePatterns[currentPattern] || SafePatterns.tile;
//     return <SafePattern />;
//   }, [currentPattern, SafePatterns]);

//   // Add debugging UI for development environment
//   function renderDebugInfo() {
//     //TODO: Replace this with InSpatial Runtime Detector Utility
//     // To make it detect and work across all environments
//     // Might want to add this feature to (InSpatial Env)
//     if (process.env.NODE_ENV !== "development") return null;

//     const debugComponent = (
//       <div className="fixed bottom-4 right-4 z-[9999] bg-black bg-opacity-80 text-white p-3 rounded-lg text-xs max-w-xs shadow-lg">
//         <h3 className="font-bold mb-1">Pattern Debug</h3>
//         <div>
//           Current: <span className="text-green-400">{currentPattern}</span>
//         </div>
//         {errorState.hasError && (
//           <div className="mt-1 text-red-400">
//             Error in: {errorState.patternName}
//             <br />
//             {errorState.errorMessage}
//           </div>
//         )}
//         <div className="mt-1">
//           Issues: {Array.from(disabledPatterns).join(", ") || "none"}
//         </div>
//         <div className="mt-2 flex gap-2">
//           <Button
//             onPointerUp={() => setDisabledPatterns(new Set())}
//             size="sm"
//             className="bg-brand w-full rounded-full text-xs cursor-pointer"
//           >
//             Reset
//           </Button>
//         </div>
//       </div>
//     );

//     // Create portal to render debug UI at the end of document body
//     // TODO: Use InSpatial Portal
//     return createPortal(debugComponent, document.body);
//   }

//   return (
//     <motion.div
//       className={kit(
//         `relative overflow-hidden z-10 flex items-center justify-center bg-inherit`,
//         className
//       )}
//       initial="initial"
//       animate="animate"
//       exit="exit"
//       variants={animationVariants[animate]}
//       transition={{ type: "tween", duration: 0.5, ease: "easeInOut" }}
//     >
//       {renderPattern()}
//       {debug === true && renderDebugInfo()}
//       {children}
//     </motion.div>
//   );
// }

// Pattern.displayName = "Pattern";
