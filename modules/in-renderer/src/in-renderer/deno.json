{
  "name": "@in/renderer",
  "license": "Apache-2.0",
  "version": "0.7.3",
  "exports": {
    ".": "./index.ts",
    "./create-renderer": "./create-renderer.ts",
    "./modes": "./modes.ts",
    "./runtime-markup": "./runtime-markup.ts",
    "./helpers": "./helpers.ts"
  },
  "tasks": {
    "lint": "deno lint",
    "fmt": "deno fmt",
    "check": "deno check **/*.ts",
    "clean": "rm -rf node_modules && rm -rf deno.lock && rm -rf dist && rm -rf testing",
    "publish": "deno publish --allow-slow-types --no-check --allow-dirty"
  },
  "publish": {
    "exclude": [".github", ".vscode", "**/*.test.ts", "test_deps.ts"]
  }
}
